"use strict";
// Prebuilt native addon for darwin-arm64 (aarch64-apple-darwin).
// This file is the entry point for the @cognee/neon-darwin-arm64 optional dep.
// Consumers should not require this directly; import via the main 'cognee' package.
module.exports = require("./cognee_neon.node");
