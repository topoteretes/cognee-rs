[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cognee-pipeline"
requires-python = ">=3.9"
version = "0.1.0"

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "."
module-name = "cognee_pipeline._native"
