# OSS / closed split — task ledger

Live checklist driving the 5-step protocol defined in
[`oss-split-implementation-prompt.md`](oss-split-implementation-prompt.md).
Plan source of truth: [`oss-split-plan.md`](oss-split-plan.md).

Status legend: `todo` → `in-progress` → `done` (with one-line result note).

| #   | Task                                                                                                  | Plan ref          | Repos  | Status |
|-----|-------------------------------------------------------------------------------------------------------|-------------------|--------|--------|
| T0  | Bootstrap: OSS worktree on `oss-split`; closed sibling repo + scaffold; task ledger; OSS still green. | §8 prereq         | both   | done — OSS worktree on `oss-split` @ `879787d`; closed repo on `main` with scaffold + `cognee-cloud-placeholder` crate (deleted at T2); OSS `cargo check --all-targets` green, closed `scripts/check_all.sh` green. |
| T1  | S1 adapter-injection audit + builder hardening.                                                        | §4 S1             | OSS    | done — audit-only: §4 S1 invariants already hold. Surviving closed-named selections (qdrant/litert) are T4 scope, `impl AclDb for DatabaseConnection` + 4 prod self-casts are T2 scope, `ExtraAuthValidator` builder is T3 scope, cloud re-exports + `default` flip are T7 scope. See [`audits/T1-s1-audit.md`](audits/T1-s1-audit.md). |
| T2  | S2 access-control extraction (trait OSS / impl closed; S2b migration cleave; S2c DB-free user; S2d remove `dyn AclDb` casts; S2e move 13 entity files + Role/Tenant). | §4 S2, S2b–e      | both   | done — **T2-pre @ 0668101**: S2c DB-free user (uuid5(NAMESPACE_OID, email) for Python parity); S2d 4 prod ACL casts → `Option<Arc<dyn AclDb>>` via new `ComponentHandles.acl_db`; 9 `UserDb::get_user(owner_id)` → `None`; `--enforce-acl` errors out; `core_migrations()` accessor; 5 test sites → `MockAclDb`. **T2-move @ a5132bc/f4274c6**: 13 auth entities + 3 traits + 3 ops + 2 models + 3 mocks + `auth.rs` + `permissions/` moved to closed `cognee-access-control` (28 OSS deletes, 41 closed creates); `impl AclDb for DatabaseConnection` removed; baseline migration cleaved; new `m20260914_000002_auth` migration on closed with `if_not_exists` + dialect-aware seeds (`pgcrypto`/`gen_random_uuid` on Postgres) + split `down()`; `pipelines/sea_orm_impl.rs` user join dropped; 7 inline `delete/src/lib.rs` ACL casts → `MockAclDb`; closed workspace `placeholder` replaced by `access-control`. **Deviation**: `cognee-http-server` gated wholesale with `#![cfg(any())]` (not surgical per-router) because ~40 files transitively reference moved types; OSS http-server is a no-op stub until T3. OSS + closed `scripts/check_all.sh` both green. |
| T3  | S3 `build_router` injection + OSS no-auth default; move 9 auth/cloud routers + `SyncRegistry`; drop `cognee-cloud` hard dep; add `with_extra_validator`. | §4 S3             | both   | done — **T3-pre @ 596cf6b**: OSS http-server un-gated; new `RouterBuilder` + `auth_resolver` (`AuthResolver`/`ExtraAuthValidator`) injection seam; 10 routers + entire `auth/` subtree + `permissions.rs` + `configuration.rs` deleted from OSS; state/wiring/components/lifecycle/openapi/permissions shrunk; main.rs restored; `cognee-cloud` hard dep + auth-only Cargo deps dropped; `cloud` removed from `default` of lib/cli/bindings-common/python/neon; 50 gated test files + auth fixtures deleted; 20 OSS-staying tests un-gated; Python `test_cloud_ops.py` flipped to expect `CogneeFeatureNotBuiltError`. **T3-move**: physical create of closed `cognee-cloud` (12 src files + tests, byte-identical to OSS pre-deletion) + `cognee-http-cloud` (~30 files: auth/ subtree + 11 routers + permissions.rs + `ClosedAuthResolver` impl, all with imports adjusted `cognee_database::auth/permissions` → `cognee_access_control::auth/permissions`, `crate::*` http-server refs → `cognee_http_server::*`); `[patch.crates-io]` added to closed workspace (qdrant tonic/hyper/tar forks); OSS `crates/cloud/` deleted wholesale; `crates/lib/Cargo.toml` cloud path repointed to closed; OSS `auth/extractor.rs` further-slimmed (dropped `RequireSuperuser`, 2-step resolver chain). OSS + closed `scripts/check_all.sh` both green. |
| T4  | S4 extract `cognee-vector-qdrant` + `cognee-llm-litert` to closed; OSS `cargo tree -e no-dev` shows zero git deps. | §4 S4             | both   | done — **T4-pre @ f9a7580**: 17 OSS test files migrated from `QdrantAdapter` to `MockVectorDB`; `http-server::wire_vector_db` repointed from qdrant to pgvector + dev-mock; default `vector_provider` flipped to pgvector. **T4-move**: created closed `cognee-vector-qdrant` (qdrant_adapter.rs + 2 tests + tests/common, git deps for qdrant v1.17.0 fork chain) and `cognee-llm-litert` (litert adapter, unpinned cognee-litert-lm git dep — T9 pins it); deleted 4 OSS adapter sources; dropped qdrant/android-litert features from 7 Cargo.tomls (lib/cli/bindings-common/python/neon/capi-capi/http-server); dropped 5 qdrant git deps + cognee-litert-lm from workspace `[workspace.dependencies]`; dropped the 3 `[patch.crates-io]` blocks (root + neon + capi); cleaved ComponentManager qdrant/litert arms (added `mock` fallback under `testing` feature for OSS smoke tests); dropped `qdrant` from `cognify`/`search`/`delete` dev-deps; updated 4 C-API smoke tests to use snake_case keys + `vector_db_provider="mock"`. **🎯 ACCEPTANCE GATE PASSED**: `cargo tree -e no-dev | grep git+` returns ZERO hits — OSS is now publishable to crates.io (modulo T9/T10 metadata + version pinning). Full OSS + closed `scripts/check_all.sh` green. |
| T5  | Pure-Rust brute-force `VectorDB` + pgvector into OSS vector defaults.                                  | §2 gap, Phase 0.5 | OSS    | done — new `cognee_vector::BruteForceVectorDB` (in-memory cosine, `tokio::sync::RwLock`, zero new deps) + 12 inline tests + integration test; `ComponentManager::init_vector_db` adds `"brute-force"` arm + `lancedb`/`qdrant` warn-fallback to brute-force (unconditional, no longer testing-gated); `Settings::default::vector_db_provider` flipped `"lancedb"` → `"pgvector"`; HTTP-server settings DTO gains `BruteForce` enum variant + UI choice list entry. OSS edge/Android profile has a working embedded vector store again. Full `scripts/check_all.sh` green. |
| T6  | S5 bindings reuse seam; isolate `ops/cloud.rs` so closed bindings depend on OSS `bindings-common` + add cloud-ops module. | §4 S5, §6.1       | both   | done — audit-only: §4 S5 invariants already hold per §6.1 reviewer addendum. `bindings-common/src/ops/cloud.rs` is cleanly liftable: zero direct `cognee_cloud::*` imports (all routes through `cognee_lib::*` re-exports), zero `pub(crate)` items needing promotion, no leaky `pub use` of cloud surface, all 3 bindings (Python/Neon/C) already call `cognee_bindings_common::ops::cloud::*` directly. Unconditional `pub mod cloud;` + body-cfg-split is intentional (centralizes `SdkError::FeatureNotBuilt`). No source-code changes. See [`audits/T6-s5-audit.md`](audits/T6-s5-audit.md). |
| T7  | S6 isolate cloud re-exports; S7 remove `cloud` from `default` features of lib, cli, bindings-common, python, neon. | §4 S6/S7          | both   | done — **S6**: deleted `crates/lib/src/api/serve.rs`, removed `pub mod serve` declaration, removed 3 cloud re-export blocks from `lib.rs` (top-level, `pub mod cloud`, prelude); dropped `cloud` feature + optional `cognee-cloud` dep from `crates/lib/Cargo.toml`. `cognee-lib` now has ZERO cognee-cloud surface. **S6 cascade**: `bindings-common/src/ops/cloud.rs` + `cli/src/commands/{serve,disconnect}.rs` rewired from `cognee_lib::*` → direct `cognee_cloud::*`; bindings-common + cli each gain their own optional `cognee-cloud` path-dep. **S7**: capi default-feature scrub finishes the job (T3-pre missed capi); python/neon/capi `cloud` features simplified to `["cognee-bindings-common/cloud"]`. **Surfaced regression from T5's flip**: `Settings::default::vector_db_provider` reverted from `"pgvector"` → `"lancedb"` (Neon/capi/python bindings don't enable `pgvector` Cargo feature; the legacy `"lancedb"` value now warn-falls-back to `BruteForceVectorDB` per T5's unconditional arm, working everywhere). 5 C-API examples updated with explicit `vector_db_provider="brute-force"`. Full `scripts/check_all.sh` green. |
| T8  | Phase-0 exit: continuous OSS-isolation CI gate + partition manifest (`scripts/split/{oss,closed}-paths.txt`) 100% coverage. | Phase 0 §8–9      | OSS    | done — added `.github/workflows/oss-isolation.yml` with 3 independent jobs: (1) per-crate `cargo check --no-default-features` across 25 OSS workspace crates, (2) `cargo tree -e no-dev \| grep git+` returns zero, (3) partition manifest assertion. `scripts/split/{check-no-default-features.sh, check-partition.sh}` host the local-runnable gates. `scripts/split/oss-paths.txt` enumerates all 1130 tracked OSS files; `closed-paths.txt` is intentionally empty (header-only) — closed sources already live in cognee-cloud-rs. Local validation green for all three gates + `scripts/check_all.sh`. **Phase 0 complete.** |
| T9  | Phase-1 metadata: per-crate description/repository/readme/keywords/categories; dual `MIT OR Apache-2.0` + add `LICENSE-MIT`. | Phase 1.1         | OSS    | done — relicensed OSS to MIT OR Apache-2.0; dual-licensed bindings (commit ef4f9f2) |
| T10 | Phase-1 readiness: `path`+`version` internal deps; docs.rs ONNX cfg; `publish = false` on test-utils/bench/examples/telemetry-emit; pin `cognee-litert-lm` rev; reserve names; `cargo publish --dry-run`; release-plz. | Phase 1.2–8       | both   | done — path+version sweep + publish=false + docs.rs + LiteRT pin + release-plz + READMEs (T10a-d: 96ea21b, 89fe2d3+c0b3ddd, 23ff543, 17e6ce0) |
| T11 | Phase-2 closed wiring: depend on OSS via `git`+`rev` with `[patch]→path` dev override; concretize inherited workspace deps in moved crates. | Phase 2.4–5       | closed | done — closed [patch] block + flip-oss-source.sh switcher (commit 4d4b1b2) |
| T12 | Phase-2 API-boundary contract tests in closed repo (AclDb wrapper, build_router injection, adapter registration). | Phase 2.6         | closed | done — 3 contract.rs files locking AclDb / RouterBuilder / Llm shapes (commit a4dffce) |
| T13 | Phase-3 OSS CI workflows (lint/test/doc/publish-dry-run/bindings + tagged publish).                    | Phase 3           | OSS    | done — publish-dry-run.yml + release-plz.yml + ci.yml --cfg docsrs (commit cb63ee4) |
| T14 | Phase-3 closed CI workflows (lint/test/build-vs-pinned-rev/private-registry publish/scheduled rev bump). | Phase 3           | closed | done — closed-ci.yml (lint+test+contract) + oss-rev-bump.yml workflow_dispatch (closed commit f72783e) |
| T15 | Phase-4 bindings distribution (OSS npm/PyPI/C; closed private registries reusing OSS op wrappers).     | Phase 4           | both   | todo   |
| T16 | **Clean-birth groundwork (reversible) — DONE.** Pushed OSS `oss-split` → `cognee-rs` `main` (private). Pushed closed `main` → `cognee-cloud-rs` (private). Both CIs green (`oss-isolation`, OSS `ci.yml` 7/7, closed `closed-ci` 6/6 incl. 3 binding-checks). Leak audit captured (`/tmp/t16/leak-audit.txt`) — closed blobs reachable in ancestry as expected; gated for the future history-cleanup pass before going public. Tagged OSS `v0.1.0` at `OSS_REV = f9739814367f93ce0e68b0294e165a71d2fe50d9`. Flipped closed manifests to `git+rev = f9739814` (closed `cargo build --workspace` green against the pin). Final `cargo publish --dry-run` topological sweep — 5 leaves clean, 17 dependents return the documented chicken-and-egg ("no matching package on crates.io") — all dep-resolution-only, no manifest issues. Every step remains undoable via repo delete / tag delete / `flip-oss-source.sh dev` / `git revert`. | §8 (push-as-is rewrite), Steps 1–4, 5 (partial), 6–7 | both | done |
| T17 | **Registry publish (irreversible — requires explicit human "go" + F8 done).** `cargo publish` 24 OSS crates to crates.io in topological order (release-plz drives); `npm publish cognee-ts` + 7 prebuilt platform packages (`ts-prebuild.yml`); `capi-release.yml` auto-attaches per-platform tarballs to the GitHub Release. Names burn forever; npm has 24h unpublish window only. PyPI explicitly removed — users build from sources via `maturin develop`. | §8 Step 5 (publish portion) | OSS | blocked-on-F8 |
| F8  | Reserve names: crates.io `cognee-rs`, npm `cognee-ts` + 7 platform pkgs. PyPI removed per user. External registry action; **hard pre-req of T17**, not T16. | post-T10 / pre-T17 | external | pending |
| F10 | Test-suite refresh for post-Phase-0 (F1/F2 era) drift. Several OSS tests assert pre-F1 constants (e.g. `DEFAULT_USER_ID = "00000000-…"`) or pre-F2 ACL behaviour (e.g. `dataset_belonging_to_other_user_collapses_to_409` expects 409 without an `AclDb` wired). Production code is correct; only test expectations are stale. Marked `#[ignore]` during T16 with F10 pointers; refresh them post-T16. | post-T16 | OSS | pending |
| F11 | Pre-T17 polish series: URL sweep `cognee-rust`→`cognee-rs` (F11a `dd5f3f2`); partition-manifest orphan fix (F11b `aaba8d8`); README Tech-Stack + Android post-T4 refresh (F11c `2a73698`); architecture/backends/configuration docs (F11d `fb90a5c`); 11 closed-router doc stubs + auth/tenants (F11e `ed37bfc`); RELEASE.md + T17 ledger consistency (F11f `f9816b6`); closed flip-oss-source.sh extension to all 10 crates (F11g closed `94ff864`); cargo doc verification (F11h, no commit — 35 unresolved-link warnings flagged to F12). | post-T16 / pre-T17 | both | done |
| F12 | Post-T17 rustdoc link cleanup. 35 unresolved intra-doc links surfaced by `cargo doc --no-deps --workspace`. Includes `AclDb::*` refs in `crates/http-server/src/routers/sessions.rs:308,405` (trait moved to closed) + ~33 others. Pre-existing; not a publish blocker but degrades crates.io rustdoc presentation. The 1796 `missing_docs` warnings are informational and out of scope. | post-T17 | OSS | pending |
| F13 | HTTP Parity Phase-1a pgvector-URL regression. Cross-SDK Docker harness fails with `pgvector adapter init: The connection string '/py/.cognee_system/vectors' cannot be parsed` since T5 (pgvector became OSS default; pre-T5 default accepted fs paths). The one-line `VECTOR_DB_PROVIDER=brute-force` patch in `helpers.py` (commit `0c03a5c`) was insufficient — `VECTOR_DB_URL` is still injected from another source (likely Python auto-config or `start_servers.sh` derivation from `SYSTEM_ROOT_DIRECTORY`). Not a T17 blocker (e2e-cross-sdk isn't a published artifact). Investigate: env exports, Python auto-config, Rust settings JSON read-path. | post-T16 / orthogonal-to-T17 | OSS | pending |
| F14 | OpenAI reasoning-model parameter compatibility. `crates/llm/src/adapters/openai.rs` has zero model-family branching — every code path forwards `temperature`/`max_tokens`/`top_p`/`frequency_penalty`/`presence_penalty` verbatim at lines 415-434 (chat), 498-503 (strict json_schema), 581-585 (function-calling fallback), 647-674 (json_object fallback). Cognify hard-codes `temperature: 0.1` at `crates/cognify/src/fact_extraction/extractor.rs:101`, `crates/cognify/src/temporal_extraction/entity_enricher.rs:74`, and `crates/cognify/src/temporal_extraction/event_extractor.rs:50`. OpenAI reasoning models (`gpt-5*`, `o1`, `o3`, `o4`) reject any `temperature != 1.0` and require `max_completion_tokens` instead of `max_tokens`. The only existing special-case is `should_disable_thinking()` at line 144 (qwen-only). Python SDK leans on `litellm` for this remapping; the Rust adapter must do it inline. **Fix**: add `is_reasoning_model()` helper that matches `gpt-5*`/`o1*`/`o3*`/`o4*` (gated on `base_url.contains("api.openai.com")` to avoid breaking custom proxies), then skip `temperature`/`top_p`/`frequency_penalty`/`presence_penalty` and rename `max_tokens` → `max_completion_tokens` when true. **LOC**: ~20-30 in `crates/llm/src/adapters/openai.rs` + ~15 LOC tests. User-reported symptom (temp 0.1 rejected with `gpt-4o-mini`, works with `gpt-5-mini`) is likely a misread of the error (`gpt-5-mini` is the model that actually rejects 0.1) — set `COGNEE_DEBUG_LLM_REQUEST=true` (line 170) to confirm. | post-T16 / 0.1.x stability | OSS | pending |
| F15 | Default vector DB needs persistence — target-split design. `BruteForceVectorDB` (`crates/vector/src/brute_force_vector_db.rs`) is the de-facto OSS default (T5 introduced it; T7's revert of `vector_db_provider = "lancedb"` warn-falls-back to it; `component_manager.rs:324-336`). It is **pure in-memory** — docstring at line 8: "**No persistence.** Data is lost on process restart." **Chosen approach**: real LanceDB adapter on `cfg(not(target_os = "android"))`, BruteForceVectorDB stays as the Android default + universal `:memory:` escape hatch. (1) New `crates/vector/src/lancedb_adapter.rs` wrapping the `lancedb` crate (Apache Arrow + embedded `*.lance` directory), default store path `{system_root_directory}/databases/cognee.lancedb` — matches Python SDK file layout, which **also resolves F13** at the root (cross-SDK harness no longer needs the `VECTOR_DB_PROVIDER=brute-force` override). (2) Replace the warn-fallback at `component_manager.rs:329-336` with a real `"lancedb"` arm gated `#[cfg(not(target_os = "android"))]`; Android keeps the brute-force fallback. (3) `lancedb` dep added to 7 Cargo.tomls (lib/cli/bindings-common/python/neon/capi/http-server) under `[target.'cfg(not(target_os = "android"))'.dependencies]`. Rejected alternatives: extend brute-force with file persistence as the universal default (the prior plan — kept as Android-only minor enhancement if needed); SQLite adapter (deferred as future `"sqlite"` provider); sled (pre-1.0). **Costs accepted**: +30-60 MB desktop/server binary, +80-120 transitive deps (arrow-rs + lance-encoding), longer compile times, ts-prebuild matrix gets heavier. **Wins**: production ANN (IVF_PQ), millions-of-vectors scale, Python file-layout parity, resolves F13. **LOC**: ~300-500 LOC adapter + ~50 LOC wiring + ~30 LOC Cargo.toml fan-out + docs touch-up (`docs/configuration.md`, `docs/roadmap/not-implemented.md:55-56`, brute-force rustdoc, T5 ledger note). | post-T16 / 0.1.x stability | OSS | pending |
