# P2-03 — Truth-subspace: vector persistence

**Status:** completed

## Goal

Give the truth-subspace centroid math a durable home in the vector layer: a
generic `upsert_raw_vectors` escape hatch on `VectorDB` for small,
system-owned vector state that must never be re-embedded, plus the two I/O
functions (`load_centroids`, `upsert_centroids`) that use it to read and write
per-dataset truth centroids. This is pure plumbing — no retriever wires it up
yet (that is Phase-2 execution, deferred per the locked decisions) — but it
must round-trip centroid payloads byte-for-byte so that when execution lands,
truth-subspace reranking is deterministic and reproducible across restarts.

## Prerequisites

- **P1-02** (vector retrieve-by-id) **has landed** (commit `08e21fc`). It added
  a **required** (no default body) method to `VectorDB` in
  `crates/vector/src/vector_db_trait.rs` with this exact signature — VERIFIED:
  ```rust
  async fn retrieve(
      &self,
      data_type: &str,
      field_name: &str,
      ids: &[Uuid],
  ) -> VectorDBResult<Vec<SearchResult>>;
  ```
  NOTE the drift from this doc's original assumption: the method is named
  `retrieve` (not `retrieve_points`), takes a `(data_type, field_name, ids)`
  triple of `&[Uuid]` (not string ids), and returns `Vec<SearchResult>` (not
  `Vec<VectorPoint>`). `SearchResult { id: Uuid, score: f32, metadata:
  HashMap<String, serde_json::Value> }` — the `metadata` field is the payload
  (`score` is a meaningless `0.0` placeholder on this path). Semantics
  (verified in the trait doc): empty `ids` → `Ok(vec![])`; **unknown
  collection → `Ok(vec![])`** (so `load_centroids` on a fresh DB with no
  `TruthCentroid_vector` collection returns empty, never errors); missing ids
  silently omitted; result order NOT guaranteed (hence the sort-by-slot).
  `load_centroids`'s call site below is written against this confirmed
  signature.
- **P2-02** (truth-subspace crate skeleton + centroid logic) **has landed**.
  The `cognee-truth-subspace` crate exists at `crates/truth-subspace/`
  (workspace member in root `Cargo.toml`). VERIFIED contents (note the drifts
  from this doc's original assumptions):
  - `src/constants.rs` — `pub const DEFAULT_K: usize = 8;` and
    `pub const TRUTH_CENTROID_COLLECTION: (&str, &str) = ("TruthCentroid",
    "vector");`. DRIFT: the collection constant is a **`(&str, &str)` tuple**,
    NOT a single `&str = "TruthCentroid_vector"`. Rust's `VectorDB` takes a
    `(data_type, field_name)` pair and derives the physical name
    `format!("{data_type}_{field_name}")` = `"TruthCentroid_vector"`
    internally. **Use the exported `TRUTH_CENTROID_COLLECTION.0` / `.1` in
    `load_centroids`/`upsert_centroids` rather than hardcoding the string
    literals `"TruthCentroid"`/`"vector"`** (single source of truth).
  - `src/models.rs` — `TruthCentroidPayload { dataset_id: String, slot: usize,
    count: usize, truth_epoch: i64, updated_at: i64, centroid: Vec<f64>,
    learning_ids: Vec<String> }` (derives `Debug, Clone, PartialEq, Serialize,
    Deserialize`; `learning_ids` has `#[serde(default)]`). DRIFT:
    `slot`/`count` are **`usize`**, not `i64` — so `centroid_id(&str, usize)`
    below takes `usize` and this doc's earlier `slot as i64` casts are wrong.
    `PartialEq` is derived, so tests can assert full-payload equality.
  - `src/align.rs` — `pub fn cosine(a: &[f64], b: &[f64]) -> f64`.
  - `src/centroids.rs` — all pure functions from `centroids.py:1-162`, incl.
    `centroid_id(dataset_id: &str, slot: usize) -> Uuid`. DRIFT: `centroid_id`
    uses the **`uuid` crate's built-in `Uuid::NAMESPACE_OID`** directly — the
    crate does **NOT** depend on `cognee-utils`. Current
    `crates/truth-subspace/Cargo.toml` deps: `sha2, uuid, serde, chrono`
    (+ dev-dep `serde_json`). No `cognee-utils`.
  - `lib.rs` re-exports `build_centroids_from_learning_vectors, centroid_id,
    centroids_changed, extend_centroids_with_learning_vectors, learning_id`
    and `DEFAULT_K, TRUTH_CENTROID_COLLECTION, TruthCentroidPayload`.
  - **STALE DOC-COMMENTS TO UPDATE**: `lib.rs`'s header says "no I/O, no
    database access" and `centroids.rs`'s header says "No I/O and no
    vector-engine dependency — `load_centroids`/`upsert_centroids` ... are
    deferred to P2-03." Adding the two I/O functions here contradicts both;
    Agent 2 must update these two doc-comments to reflect that centroids.rs
    now carries the vector-engine-backed persistence pair.

  This task's job is exactly the two functions Python defines at
  `centroids.py:165-194` (`load_centroids`, `upsert_centroids`), which P2-02
  deliberately left out because they need the vector-DB dependency (P2-02's own
  `centroids.rs` header explicitly defers them here). Add them to the same
  `crates/truth-subspace/src/centroids.rs` file and add `cognee-vector` as a
  dependency of `crates/truth-subspace/Cargo.toml` (`path = "../vector",
  version = "0.1.3"`, mirroring how `crates/vector/Cargo.toml` declares its
  own `cognee-utils` path-dep with an explicit workspace version).

  **CYCLE-SAFETY (verified — this is the key architecture decision):** the
  dependency edge is one-directional `cognee-truth-subspace → cognee-vector`.
  `crates/vector/Cargo.toml` currently depends only on `cognee-utils` (plus
  dev-deps `cognee-test-utils`/`cognee-models`) and does **not** depend on
  `cognee-truth-subspace`, so adding this edge creates **no cycle**. Do NOT
  invert this by putting the persistence glue in `cognee-vector` — that would
  force `cognee-vector` to depend on `cognee-truth-subspace` for
  `TruthCentroidPayload`, diverging from Python (which keeps this glue in the
  `truth_subspace` module) and coupling the vector crate to truth-subspace.
  The accepted cost is that `cognee-truth-subspace` is no longer "pure math" —
  it now transitively pulls tokio/async-trait/lancedb/arrow via
  `cognee-vector`; this is expected and matches Python's module layout.

## Files touched

- `crates/vector/src/vector_db_trait.rs` (edit) — add `upsert_raw_vectors` to
  the `VectorDB` trait (default body returns an error, matching Python's
  `raise NotImplementedError`).
- `crates/vector/src/mock_vector_db.rs` (edit) — implement
  `upsert_raw_vectors` for `MockVectorDB`.
- `crates/vector/src/brute_force_vector_db.rs` (edit) — implement
  `upsert_raw_vectors` for `BruteForceVectorDB`.
- `crates/vector/src/lancedb_adapter.rs` (edit) — implement
  `upsert_raw_vectors` for `LanceDbAdapter` (`#[cfg(not(target_os =
  "android"))]`).
- `crates/vector/src/pgvector_adapter.rs` (edit) — implement
  `upsert_raw_vectors` for `PgVectorAdapter` (`#[cfg(feature = "pgvector")]`).
- `crates/vector/src/error.rs` (edit, if needed) — reuse
  `VectorDBError::StorageError` for the default-impl "unsupported" case; no
  new variant is required unless an adapter needs a more specific one.
- `crates/truth-subspace/src/centroids.rs` (edit, assumed created by P2-02) —
  add `load_centroids` and `upsert_centroids`.
- `crates/truth-subspace/src/lib.rs` (edit, assumed created by P2-02) —
  re-export `load_centroids`/`upsert_centroids`.
- `crates/truth-subspace/Cargo.toml` (edit, assumed created by P2-02) — add
  `cognee-vector` path dependency.
- `crates/vector/tests/brute_force_integration.rs` (edit) — add an
  `upsert_raw_vectors` round-trip case.
- `crates/vector/tests/pgvector_integration.rs` (edit) — add an
  `upsert_raw_vectors` round-trip case (Postgres-gated, existing pattern of
  per-test throwaway databases per commit `a812a5b`).
- `crates/truth-subspace/tests/centroids_persistence.rs` (new) — integration
  test for `load_centroids`/`upsert_centroids` against `BruteForceVectorDB`
  and `MockVectorDB`.

## Implementation steps

1. **Trait method.** In `crates/vector/src/vector_db_trait.rs`, add:
   ```rust
   async fn upsert_raw_vectors(
       &self,
       data_type: &str,
       field_name: &str,
       points: &[VectorPoint],
   ) -> VectorDBResult<()> {
       let _ = (data_type, field_name, points);
       Err(VectorDBError::StorageError(
           "upsert_raw_vectors is not implemented for this adapter".to_string(),
       ))
   }
   ```
   Place it near `index_points` (after `delete_points` is a reasonable
   location, following the existing default-impl style used for
   `delete_points`/`list_collections`/`prune`/`batch_search_similar`). Document
   on the method that this is for small, system-owned vector state (centroids,
   not content) where the caller has already computed the vector and
   re-embedding from text would be wrong — mirroring the Python docstring at
   `vector_db_interface.py:66-79` (Python's base `upsert_raw_vectors` likewise
   `raise NotImplementedError`; only its LanceDBAdapter overrides it — Rust
   goes further and overrides all four real adapters).

   **REQUIRED-vs-DEFAULTED — this method is DEFAULTED (has a default body).**
   Unlike P1-02's `retrieve` (which is required — no default body — and so
   forced a stub impl on every one of the ~9 in-tree `VectorDB` test doubles),
   `upsert_raw_vectors` carries the error-returning default above. Therefore
   the **only** implementors to touch are the four real adapters (Mock,
   BruteForce, LanceDb, PgVector). The verified full `impl VectorDB for` census
   is: 4 real adapters in `crates/vector/src/`; 8 `TestVectorDb` doubles in
   `crates/search/src/` (`graph_completion_retriever`,
   `advanced_graph_retrievers`, `chunks_retriever`, `temporal_retriever`,
   `summaries_retriever`, `completion_retriever`, `triplet_retriever`,
   `orchestration/search_execution_builder`); and 1 `FailingVectorDB` in
   `crates/http-server/src/health.rs`. **The 9 test doubles inherit the default
   and need NO changes** — do not touch them (they never call
   `upsert_raw_vectors`, and if they did the default's error is the correct
   "unsupported" behavior).

   Each point is a full `VectorPoint { id, vector, metadata }` — reuse the
   existing type rather than inventing a `{id, vector, payload}` struct; the
   Python `points: list[dict]` with `id`/`vector`/`payload` keys
   (`centroids.py:186-190`) maps directly onto `VectorPoint`'s
   `id`/`vector`/`metadata` fields. There is no Rust equivalent of Python's
   `payload_schema: Optional[Any]` parameter — Rust has no adapter-level
   runtime schema validation hook, and validation instead happens where the
   caller deserializes `TruthCentroidPayload` from the retrieved metadata (see
   Parity notes). Drop the parameter from the Rust signature.

2. **`MockVectorDB::upsert_raw_vectors`.** Implement as an unconditional
   insert-or-replace by id into the `(data_type, field_name)` collection
   (create the collection lazily with the incoming vector's dimension if it
   does not exist yet — Python's adapters likewise do not require
   `create_collection` to have been called first for this path since the
   caller may be seeding a brand-new `TruthCentroid_vector` collection).
   **Do not** call the dataset-membership merge helper used by
   `index_points` (`VectorPoint::merge_dataset_membership`) — centroid points
   are not content-addressed across datasets, each `centroid_id(dataset_id,
   slot)` already scopes one dataset, so union-of-membership semantics do not
   apply. A plain full-metadata replace is correct and matches Python's
   `create_data_points`-free direct write in `upsert_centroids`.

3. **`BruteForceVectorDB::upsert_raw_vectors`.** Same semantics as step 2:
   lazily create the collection (reuse the existing `Self::key(data_type,
   field_name)` helper), replace any existing point with the same id in the
   `Vec<VectorPoint>` (or append if absent), skip the
   `merge_dataset_membership` call that `index_points` performs.

4. **`LanceDbAdapter::upsert_raw_vectors`.** Reuse the existing
   `collection_name(data_type, field_name)` helper and the same Arrow
   `RecordBatch` construction path `index_points` uses (id / vector / metadata
   columns), but skip whatever dataset-membership-union step `index_points`
   performs before writing, and open-or-create the table if it does not exist
   yet (mirrors `index_points`'s existing "create table on first write"
   behavior for a new `(data_type, field_name)` — reuse it if `index_points`
   already lazily creates; otherwise call `create_collection` first with the
   incoming vector's dimension).

5. **`PgVectorAdapter::upsert_raw_vectors`.** Reuse
   `Self::collection_name(data_type, field_name)` and the same
   `INSERT ... ON CONFLICT (id) DO UPDATE SET vector = EXCLUDED.vector,
   metadata = EXCLUDED.metadata` batched-insert pattern `index_points` uses
   (`pgvector_adapter.rs` around the `BATCH_SIZE`-chunked loop), but do
   **not** call `fetch_metadata`/`merge_dataset_membership` first — write the
   incoming metadata verbatim. Create the table via `create_collection` if
   `has_collection` returns false (the `TruthCentroid_vector` collection will
   not exist on first write since nothing else creates it).

6. **`load_centroids`** in `crates/truth-subspace/src/centroids.rs` (parity
   target: `centroids.py:165-176`):
   ```rust
   pub async fn load_centroids(
       vector: &dyn VectorDB,
       dataset_id: &str,
       k: usize,
   ) -> VectorDBResult<Vec<TruthCentroidPayload>> {
       let (data_type, field_name) = TRUTH_CENTROID_COLLECTION; // ("TruthCentroid", "vector")
       let ids: Vec<Uuid> = (0..k).map(|slot| centroid_id(dataset_id, slot)).collect();
       // P1-02 method: retrieve(...) -> Vec<SearchResult>, unknown-collection -> Ok(vec![]).
       let rows = vector.retrieve(data_type, field_name, &ids).await?;
       let mut centroids: Vec<TruthCentroidPayload> = rows
           .into_iter()
           .filter_map(|row| serde_json::from_value(json_from_metadata(&row.metadata)).ok())
           .filter(|c: &TruthCentroidPayload| c.dataset_id == dataset_id)
           .collect();
       centroids.sort_by_key(|c| c.slot);
       Ok(centroids)
   }
   ```
   `centroid_id`'s second arg is `usize` (the `0..k` loop var directly — NOT
   `slot as i64`). Use the `TRUTH_CENTROID_COLLECTION` tuple constant so the
   derived collection name (`"{data_type}_{field_name}"`, the shared convention
   across `MockVectorDB::collection_key`, `BruteForceVectorDB::key`,
   `lancedb_adapter::collection_name`, `PgVectorAdapter::collection_name`)
   is exactly `TruthCentroid_vector`, matching Python's
   `TRUTH_CENTROID_COLLECTION` string literally. `retrieve` returns
   `Vec<SearchResult>`; deserialize each `SearchResult.metadata` map as
   `TruthCentroidPayload` (build a `serde_json::Value::Object` from the
   `HashMap<String, Value>` first — `json_from_metadata` is a small local
   helper, `serde_json::Value::Object(map.clone().into_iter().collect())`);
   silently skip rows that fail to parse (mirrors Python's `if not
   isinstance(payload, dict): continue` guard at `centroids.py:171-172` — the
   Rust equivalent failure mode is a deserialize error, treated the same way).
   Keep the defensive `dataset_id` equality filter even though `centroid_id`
   already scopes the id to one dataset — it guards against a hash collision
   or stale/corrupted payload, exactly as Python does at `centroids.py:174`.

7. **`upsert_centroids`** (parity target: `centroids.py:179-194`):
   ```rust
   pub async fn upsert_centroids(
       vector: &dyn VectorDB,
       centroids: &[TruthCentroidPayload],
   ) -> VectorDBResult<()> {
       if centroids.is_empty() {
           return Ok(());
       }
       let (data_type, field_name) = TRUTH_CENTROID_COLLECTION;
       let points: Vec<VectorPoint> = centroids
           .iter()
           .map(|c| {
               let id = centroid_id(&c.dataset_id, c.slot); // c.slot is usize — no cast
               let vector = c.centroid.iter().map(|v| *v as f32).collect();
               let metadata = metadata_from_payload(c); // serde_json::to_value(c) as HashMap
               VectorPoint { id, vector, metadata }
           })
           .collect();
       vector
           .upsert_raw_vectors(data_type, field_name, &points)
           .await
   }
   ```
   Note the `f64` → `f32` narrowing: `TruthCentroidPayload.centroid` is
   `Vec<f64>` (Python `list[float]`) but `VectorPoint.vector` is `Vec<f32>` —
   cast on the way in/out and document the precision loss (see Parity notes).

8. Add both functions to `crates/truth-subspace/src/lib.rs`'s public surface
   next to whatever P2-02 already re-exports (`centroid_id`, `normalize`,
   `weighted_centroid`, etc.).

9. Run `cargo fmt`, `cargo check --all-targets`, `cargo clippy --all-targets`,
   then `cargo test -p cognee-vector -p cognee-truth-subspace`, then
   `scripts/check_all.sh`.

## Parity notes

- **Collection name**: `TruthCentroid_vector` must be produced by Rust's
  `"{data_type}_{field_name}"` convention with `data_type = "TruthCentroid"`,
  `field_name = "vector"` — an exact string match to Python's
  `TRUTH_CENTROID_COLLECTION` (`constants.py:1`), so a future cross-SDK
  read/write test on this collection is possible even though it is out of
  scope for this task.
- **Centroid id formula**: `uuid5(NAMESPACE_OID, f"TruthCentroid:{dataset_id}:{slot}")`
  (`centroids.py:20-21`) → Rust `Uuid::new_v5(&NAMESPACE_OID,
  format!("TruthCentroid:{dataset_id}:{slot}").as_bytes())` — this is P2-02's
  `centroid_id` fn, reused as-is here; confirm `dataset_id` is formatted with
  `.to_string()` on a `Uuid` (or passed through as `&str` if the caller already
  has a string) so the seed bytes are byte-identical to Python's
  `str(dataset_id)` interpolation.
- **`load_centroids` slot range**: build ids for `slot in 0..k` (`k` defaults
  to `DEFAULT_K = 8`), not "however many centroids exist" — a dataset with
  fewer than `k` centroids simply gets fewer `retrieve`
  hits, which the Rust retrieve-by-id method (`retrieve`) already handles by
  omitting misses from its result `Vec` (matching Python's `rows` list, which
  is however many of the `k` ids resolved).
- **Sort by slot**: `sorted(centroids, key=lambda centroid:
  centroid.slot)` (`centroids.py:176`) → Rust `.sort_by_key(|c| c.slot)`.
  Slot is not guaranteed to come back in order from the vector store since
  ids are looked up individually, not range-scanned.
- **Dataset-id defensive filter**: Python re-checks `centroid.dataset_id ==
  str(dataset_id)` after `model_validate` (`centroids.py:172-174`) even
  though the id already encodes the dataset — preserve this exactly, do not
  optimize it away.
- **`upsert_centroids` empty guard**: `if not centroids: return` (
  `centroids.py:180-181`) → Rust must return `Ok(())` immediately for an
  empty slice without calling `upsert_raw_vectors` at all (avoids a
  needless empty-batch round trip, and avoids adapters that assert
  non-empty `points`).
- **Payload symmetry**: Python's `points` payload is
  `centroid.model_dump()` — the *entire* `TruthCentroidPayload`, including
  the `centroid` vector itself, duplicated inside the JSON payload alongside
  the raw vector column (`centroids.py:184-190`). Rust must do the same: the
  `VectorPoint.metadata` JSON blob for a centroid point contains the full
  `TruthCentroidPayload` (including its own `centroid: Vec<f64>` field),
  which is redundant with `VectorPoint.vector` but intentional — Python keeps
  it so a plain `retrieve()` without a paired ANN query still yields a
  self-describing row.
- **`f64` vs `f32` divergence (Rust-only, intentional)**: Python vectors are
  untyped `list[float]` (i.e. `f64`-precision in effect); Rust's `VectorPoint.
  vector` is `Vec<f32>` throughout the vector crate (see
  `crates/vector/src/models.rs:12`). `upsert_centroids` narrows `f64 → f32`
  when building the `VectorPoint`, and `load_centroids` reconstructs
  `TruthCentroidPayload.centroid: Vec<f64>` from the deserialized JSON
  payload (not from the narrowed `VectorPoint.vector`), so the *payload*
  round-trips at full `f64` precision even though the ANN-searchable vector
  column is `f32`. Document this as an accepted precision divergence — it
  does not affect structural parity (slot assignment, cosine nearest-slot
  choice) at the tolerances this project already accepts for cognify
  (locked decision #2).
- **No dataset-membership union**: unlike `index_points`
  (`VectorPoint::merge_dataset_membership`, `crates/vector/src/models.rs:71-94`),
  `upsert_raw_vectors` must **not** merge `dataset_ids`/`dataset_id` metadata
  from a previous point at the same id. Python's `upsert_centroids` has no
  such concept at all (`vector_engine.upsert_raw_vectors` is a raw write);
  introducing a merge step here would be a Rust-only bug, not a port of
  anything in Python.
- **`payload_schema` is inert in Rust**: Python threads
  `payload_schema: Optional[Any] = None` through to `upsert_raw_vectors` and
  passes `TruthCentroidPayload` as that argument (`centroids.py:192`) — some
  Python adapters use it for provider-side schema declaration. No Rust
  adapter needs this (schema is enforced by `serde` deserialization at the
  `load_centroids` call site), so the parameter is dropped entirely rather
  than threaded through and ignored. Document this as an accepted API
  divergence in the trait doc-comment.

## Testing

- `crates/vector/tests/brute_force_integration.rs`: add
  `upsert_raw_vectors_round_trips_via_retrieve` — upsert two points
  with distinct metadata payloads into a fresh collection (no prior
  `create_collection` call), then use the P1-02 `retrieve` method to read
  them back and assert vector + metadata equality; add a second case that
  upserts the *same* id twice with different metadata and asserts the second
  write fully replaces the first (no dataset-membership merge occurred).
- `crates/vector/tests/pgvector_integration.rs`: same two cases against
  `PgVectorAdapter`, using the existing per-test throwaway-database harness
  (commit `a812a5b`).
- `MockVectorDB`: inline `#[cfg(all(test, feature = "testing"))]` unit test
  in `crates/vector/src/mock_vector_db.rs` mirroring the brute-force case
  (kept fast, no I/O).
- `crates/truth-subspace/tests/centroids_persistence.rs` (new):
  - `upsert_then_load_round_trips_all_fields` — build a `Vec<TruthCentroidPayload>`
    (via P2-02's `build_centroids_from_learning_vectors`), `upsert_centroids`
    into a `BruteForceVectorDB`, `load_centroids` back, and assert every field
    (`slot`, `count`, `truth_epoch`, `updated_at`, `learning_ids`, and
    `centroid` values within the accepted `f32` narrowing tolerance) matches.
  - `load_centroids_sorts_by_slot` — upsert centroids in a shuffled slot
    order, assert `load_centroids` returns them sorted `0..k`.
  - `load_centroids_filters_other_datasets` — upsert centroids for two
    different `dataset_id`s into the same `TruthCentroid_vector` collection,
    assert `load_centroids(dataset_a)` never returns a `dataset_b` row.
  - `load_centroids_partial_slots` — upsert only 3 of `k=8` slots, assert
    `load_centroids` returns exactly those 3, sorted, and does not error on
    the 5 missing ids.
  - `upsert_centroids_empty_is_noop` — call `upsert_centroids(&vector, &[])`
    against a `VectorDB` that would error on any real write (e.g. a mock
    configured via `set_index_error`-style injection, or simply assert no
    collection is created), confirming the empty-guard short-circuits before
    any adapter call.

## Checkpoints / acceptance

- [x] `cargo check --all-targets` passes.
- [x] `cargo clippy --all-targets -- -D warnings` passes (no `unwrap()` in
      non-test code; any `expect()` carries a why-comment).
- [x] `cargo test -p cognee-vector` passes, including the new
      `upsert_raw_vectors` round-trip cases for mock/brute-force/pgvector.
- [x] `cargo test -p cognee-truth-subspace` passes, including all five new
      `load_centroids`/`upsert_centroids` cases.
- [x] `scripts/check_all.sh` passes end-to-end (fmt, check, clippy, C/Python/
      TS/Java binding checks).
- [ ] `load_centroids`/`upsert_centroids` are reachable only from
      `cognee-truth-subspace`'s public API — nothing in `crates/search` or
      `crates/cognify` calls them yet (Phase-2 execution wiring is a later,
      separate task).
- [x] Manual round-trip sanity check: upsert 8 centroids for one dataset,
      load them back, confirm `slot` values are exactly `0..7` in order and
      `dataset_id` matches on every returned row.

## Risks & open items

- **P1-02 method name/signature — RESOLVED (verified against landed commit
  `08e21fc`):** the method is `retrieve(&self, data_type: &str, field_name:
  &str, ids: &[Uuid]) -> VectorDBResult<Vec<SearchResult>>`, required (no
  default). Step 6 and the Prerequisites bullet have been corrected to this.
- **P2-02 crate layout — RESOLVED (verified):** crate at
  `crates/truth-subspace/` with `constants.rs`/`models.rs`/`align.rs`/
  `centroids.rs`/`lib.rs`; no `tests/` dir yet (this task adds
  `tests/centroids_persistence.rs`). Field-type / constant / dependency
  drifts are captured in the corrected Prerequisites bullet above
  (`slot`/`count` are `usize`; `TRUTH_CENTROID_COLLECTION` is a tuple; the
  crate uses `Uuid::NAMESPACE_OID`, not `cognee-utils`).
- **Collection creation from `upsert_raw_vectors` on a brand-new
  collection — RESOLVED (verified):** NONE of the four adapters lazily
  create the collection inside `index_points`, so every `upsert_raw_vectors`
  MUST create the `(data_type, field_name)` collection itself (with the
  incoming vector's dimension) when it does not exist yet — nothing else ever
  creates `TruthCentroid_vector`. Verified behaviors:
  - `BruteForceVectorDB::index_points` → `get_mut(&key).ok_or(CollectionNotFound)`.
  - `MockVectorDB::index_points` → same `get_mut(...).ok_or(CollectionNotFound)`.
  - `LanceDbAdapter::index_points` → `connection.open_table(&name)` (errors if
    absent) and `resolved_dimension(&name)` also requires the table; its
    `create_collection` guards with `has_collection` and calls
    `create_empty_table`.
  - `PgVectorAdapter::index_points` → straight `INSERT ... ON CONFLICT (id) DO
    UPDATE` with no existence check (the raw SQL would fail on a missing
    table); its `create_collection` guards with `has_collection`.
  So each adapter's `upsert_raw_vectors` should: create the collection first
  if `has_collection`/`get`-miss (for LanceDb/PgVector call `create_collection`
  with `points[0].vector.len()`; for the in-memory pair insert a fresh
  `Collection`/entry with that dimension), then do the by-id
  insert-or-replace **without** `merge_dataset_membership` /
  `fetch_metadata` (steps 2-5). Guard the empty-`points` case too (mirror
  `index_points`' `if points.is_empty() { return Ok(()) }`), though
  `upsert_centroids`'s empty-guard already prevents an empty call in practice.
- **`f64`→`f32` precision narrowing** on the vector column is an accepted,
  documented divergence (see Parity notes) but has not been validated against
  the actual nearest-slot cosine assignment at Phase-2 execution time; if a
  future structural-parity run shows slot misassignment near a tie, this is
  the first place to look.
- **No dataset scoping at the collection level**: all datasets' centroids
  live in one shared `TruthCentroid_vector` collection, disambiguated only by
  the `dataset_id`-scoped id and the defensive payload filter. This matches
  Python exactly, but means a `list_collections()`/`prune()` call touches
  every dataset's centroids at once — acceptable for now, flagged here in
  case a future multi-tenant isolation requirement wants per-dataset
  collections instead.
- **No migration for pre-existing installs**: since no prior Rust code
  writes to `TruthCentroid_vector`, there is nothing to backfill — this is
  the "existing datasets: documented degradation, no backfill" locked
  decision applied trivially (there is no old data to degrade).
