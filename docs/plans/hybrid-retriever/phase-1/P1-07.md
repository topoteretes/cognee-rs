# P1-07 — Hybrid module — chunk + BM25 lane

**Status:** completed

## Goal

Port the Python `cognee/modules/retrieval/hybrid/{results,pairs,ranking,chunks}.py` files
into a new `crates/search/src/retrievers/hybrid/` module, giving the future `HybridRetriever`
(Phase 1, `SearchType::HybridCompletion`) its chunk-ranking core: a per-query Okapi BM25
lexical lane merged with the existing vector `DocumentChunk_text` / `TextSummary_text`
channels via Reciprocal Rank Fusion (RRF), with optional importance-weight boosting. This
task delivers the pure ranking/merge logic and its vector-orchestration wrapper only — it
does **not** wire a `SearchRetriever` impl, `SearchType` variant, or wire-level params (those
land in a later task).

## Prerequisites

**All of P1-01..P1-06 have landed on `feat/hybrid-retriever-port` (verified against committed
code — the interface shapes below are the *actual* committed signatures, not assumptions).**

- **P1-02 (landed)** — batched retrieve-by-ids on the `VectorDB` trait. **Exact committed
  signature** (`crates/vector/src/vector_db_trait.rs:109-114`):
  `async fn retrieve(&self, data_type: &str, field_name: &str, ids: &[Uuid]) -> VectorDBResult<Vec<SearchResult>>`
  — a *required* trait method (not defaulted). Semantics (from the committed doc-comment):
  empty `ids` → `Ok(vec![])` without touching storage; **unknown collection → `Ok(vec![])`**
  (deliberate divergence from `search_similar`, which returns `CollectionNotFound`); missing
  ids silently omitted; **result order not guaranteed** — re-index by `SearchResult::id` if
  order matters. This matches exactly what `chunks.rs`'s
  `load_source_chunks_for_summaries` / `load_summary_text_for_ranked_pairs` need. Note the
  "unknown collection → []" behavior means the `has_collection` pre-check in
  `load_summary_text_for_ranked_pairs` is *optional* for correctness (retrieve already
  tolerates a missing `TextSummary_text` collection), but keeping it as an early-return is
  still fine.
- **P1-03 (landed) — SHAPE DIFFERS FROM THE ORIGINAL ASSUMPTION.** The BM25 lexical lane
  landed as a **free `pub(crate)` function**, *not* a `BM25ChunksRetriever` struct/constructor.
  **Exact committed signature** (`crates/search/src/retrievers/bm25.rs:258-262`, re-exported as
  `crate::retrievers::bm25_scored_chunks` via `retrievers/mod.rs:24`):
  `pub(crate) async fn bm25_scored_chunks(graph_db: &Arc<dyn GraphDBTrait>, query: &str, limit: usize) -> Vec<(serde_json::Value, f32)>`.
  It takes a **`&Arc<dyn GraphDBTrait>`** (NOT a vector DB and NOT a constructed retriever
  object), and it *already* does internally: fresh per-call corpus rebuild (no cache — decision
  5), Okapi BM25 `k1=1.5 b=0.75` with `idf = ln(1 + (N-df+0.5)/(df+0.5))` summed over unique
  query terms, `\w+`+`DEFAULT_STOP_WORDS` tokenization, `limit == 0` → `[]`, **truncate-to-limit
  then drop non-positive scores**, and `payload["id"]` backfill. It does **NOT** apply node-name
  filtering or the `chunks_top_k * 2` doubling (caller's job — confirmed by the bm25.rs module
  docstring). **Consequence for this task:** wherever the steps below say `bm25_lane:
  &BM25ChunksRetriever`, that is wrong — the parameter must be `graph_db: &Arc<dyn GraphDBTrait>`,
  and `search_bm25_chunks` calls `crate::retrievers::bm25_scored_chunks(graph_db, query, limit)`
  directly. There is no BM25 object to construct or mock; tests drive the BM25 lane by populating
  a `MockGraphDB` with `DocumentChunk` nodes.
- **P1-04 (landed) — `importance_weight` lives on `DataPoint`, not `DocumentChunk`.** (The
  original draft misattributed this to "P1-05 on the `DocumentChunk` model" — corrected here.)
  `crates/models/src/data_point.rs:106-107` adds
  `#[serde(default = "default_importance_weight")] pub importance_weight: Option<f64>` with a
  `0.5` default (Python-faithful; `default_importance_weight` at line 22). `DataPoint::new` /
  `with_metadata` initialize it to `Some(0.5)`, and `classify_documents`
  (`crates/models/src/document.rs:130`) propagates it. Because `DocumentChunk` flattens a
  `DataPoint`, the field appears in the chunk's vector-point metadata read by `ranking.rs`.
  `ranking.rs` still reads it defensively from the JSON payload (missing/non-numeric → `0.5`),
  so it neither hard-depends on the exact struct location nor breaks on legacy payloads.
- **P1-05 (landed) — shared helpers this task consumes.** `TextSummary.source_chunk_id:
  Option<Uuid>` (set equal to `made_from` at construction) exists at
  `crates/cognify/src/summarization/models.rs:47`, and the `uuid5(chunk_id, "TextSummary")` id
  scheme is at `models.rs:73` (`Uuid::new_v5(&chunk_id, b"TextSummary")`) — this is the scheme
  `pairs::summary_id_for_chunk` must mirror locally. `DEFAULT_HYBRID_USER_PROMPT_TEMPLATE` is
  exported from `crates/search/src/utils` (`utils/mod.rs:8-12`; not used by this task but part
  of the same landed change), and `EdgeType::retrieval_text`
  (`crates/models/src/edge_type.rs:59`) is available for P1-08. `DEFAULT_STOP_WORDS` is
  `pub(crate)` in `utils` (`utils/mod.rs:14`) and already consumed by the BM25 lane.

State (verified now): `crates/search` compiles against `cognee-vector`, `cognee-models`, and
`cognee-graph`. No `hybrid` module exists yet under `crates/search/src/retrievers/` (verified:
`find crates/search -iname '*hybrid*'` returns nothing).

## Files touched

- `crates/search/src/retrievers/hybrid/mod.rs` (new) — `pub(crate) mod` wiring for
  `results`, `pairs`, `ranking`, `chunks`; re-exports `retrieve_hybrid_chunks` and
  `HybridChunksResult` for the future retriever task.
- `crates/search/src/retrievers/hybrid/results.rs` (new) — payload/display/id helpers +
  node-set filter predicate.
- `crates/search/src/retrievers/hybrid/pairs.rs` (new) — chunk↔summary pairing and merge.
- `crates/search/src/retrievers/hybrid/ranking.rs` (new) — RRF + importance-weight scoring.
- `crates/search/src/retrievers/hybrid/chunks.rs` (new) — three-lane orchestration
  (`retrieve_hybrid_chunks`, `search_collection`, `search_bm25_chunks`, source-chunk/summary
  backfill).
- `crates/search/src/retrievers/mod.rs` (edit) — add `mod hybrid;` (verified: file currently
  lists 13 `mod` declarations, lines 1-13 — `bm25` was added at line 3 by P1-03 — with no
  `hybrid` entry; the BM25 lane is already re-exported at line 24 as
  `pub(crate) use bm25::bm25_scored_chunks;`, which the hybrid module calls via
  `crate::retrievers::bm25_scored_chunks`).
- `crates/search/Cargo.toml` (edit) — add `"v5"` to the `uuid` dependency's `features` list
  (currently `features = ["serde", "v4"]` at line 42 — no `v5`, needed for
  `Uuid::new_v5` in `pairs::summary_id_for_chunk`); add `"macros"` to the `tokio` dependency's
  `features` list (currently `features = ["sync"]` at line 35 — no `macros`, needed for
  `tokio::try_join!` in `chunks::retrieve_hybrid_chunks`).

## Implementation steps

1. **Scaffold the module.** Create `crates/search/src/retrievers/hybrid/mod.rs` with
   `mod results; mod pairs; mod ranking; mod chunks;` and re-export the public surface this
   task defines (`pub(crate) use chunks::{retrieve_hybrid_chunks, HybridChunksResult};`). Add
   `mod hybrid;` to `crates/search/src/retrievers/mod.rs` (keep it private/`pub(crate)` — no
   `SearchRetriever` impl lives here yet, so nothing needs crate-external visibility).

2. **`results.rs` — port `hybrid/results.py` (60 lines) onto `SearchItem`.** Rust already has
   a normalized result shape at `crates/search/src/types/search_result.rs:9-13`
   (`SearchItem { id: Option<Uuid>, score: Option<f32>, payload: Value }`) and a converter
   `search_results_to_context` (`crates/search/src/retrievers/context_items.rs:5-19`) that
   turns `cognee_vector::SearchResult` into `Vec<SearchItem>`. Since every lane in this task
   (BM25, vector, summary) can be normalized to `SearchItem` at its call site, Python's
   duck-typed `Any` collapses to `&SearchItem` — port the six functions as:
   - `payload(item: &SearchItem) -> &serde_json::Value` — return `&item.payload` (Python
     `results.py:5-9` defensively falls back to `{}` for non-dict input; Rust's `SearchItem`
     always carries a `Value`, so add a small `as_object_or_empty` helper only where callers
     need to iterate keys, e.g. `belongs_to_set` lookup).
   - `display_value(value: &Value) -> Option<String>` — mirror `results.py:12-18`: `Null` →
     `None`; `String`/`Number`/`Bool` → trimmed string, empty → `None`; `Array`/`Object` →
     `None`. (Python also accepts a bare `UUID` object; Rust ids are always JSON strings by
     the time they reach `payload`, so that branch is unreachable and can be omitted.)
   - `result_id(item: &SearchItem) -> Option<String>` — `display_value(payload(item).get("id"))`
     or fall back to `item.id.map(|u| u.to_string())`, matching `results.py:21-23`.
   - `scored_payload((payload, score): (Value, f32)) -> (Value, f32)` — Rust's BM25 lane
     (P1-03) is statically typed, so the dynamic-shape guard from `results.py:26-32` is
     vestigial; keep the function only to clamp a `NaN`/negative score to `0.0` for defensive
     parity, called from `search_bm25_chunks`.
   - `payload_matches_node_filter(payload: &Value, node_name: Option<&[String]>, operator: &str) -> bool`
     — port `results.py:35-51`. **Divergence from Python:** Rust's `belongs_to_set` entries are
     JSON values built at `crates/models/src/document.rs:148-178` as
     `{"id":..,"name":..,"type":"NodeSet"}` objects (not the plain dataset-id strings Python
     stores), except the dataset-membership form emitted by `DataPoint::new` /
     `document_chunk.rs`, which is still a bare string
     (`crates/models/src/data_point.rs` — `belongs_to_set: dataset_id.map(|id| vec![json!(id.to_string())])`).
     So the entry-to-name step must be: if the entry is a JSON string, use it as-is; if it is
     an object, read `.name` as a string and skip the entry if absent/non-string; otherwise
     skip. Build the resulting `HashSet<String>` and apply `AND` (`issubset`) vs `OR`
     (non-empty intersection) exactly as Python (only the literal string `"AND"` selects
     AND semantics; anything else, including unrecognized values, behaves as `OR` — no
     validation/error path, matching `results.py:49-51`).
   - `first_display_value(values: &[&Value]) -> Option<String>` — port `results.py:54-59`.

3. **`pairs.rs` — port `hybrid/pairs.py` (132 lines).** Define a `ChunkSummaryPair` struct
   (Rust struct instead of Python's ad-hoc dict) with fields `chunk_id: Option<String>`,
   `chunk_text: Option<String>`, `summary_id: Option<String>`, `summary_text: Option<String>`,
   `chunk: Option<SearchItem>`, `bm25_rank: Option<usize>`, `vector_rank: Option<usize>`,
   `summary_rank: Option<usize>`. Port:
   - `chunk_summary_pairs(bm25_chunks: &[SearchItem], vector_chunks: &[SearchItem], summary_hits: &[SearchItem], node_name: Option<&[String]>, operator: &str) -> Vec<ChunkSummaryPair>`
     — two-phase loop exactly as `pairs.py:15-65`: first iterate `(bm25_rank, bm25_chunks)`
     then `(vector_rank, vector_chunks)`, calling `_find_chunk_summary_pair` /
     `_new_chunk_summary_pair`, applying the "first-rank-wins" rule (`pair[rank_field]` set
     only if currently `None`) and the id-adoption rule when a text-merged, id-less BM25
     payload later gets an id from a matching vector chunk (`pairs.py:37-40`). Then iterate
     `summary_hits`, applying `payload_matches_node_filter` per hit (`pairs.py:46-47`,
     skip on mismatch), require `source_chunk_id` (skip + log a warning if absent, mirroring
     `pairs.py:49-54`), and merge by `chunk_id` only (never by text — summaries are always
     looked up by id).
   - `source_chunk_ids_to_load(pairs: &[ChunkSummaryPair]) -> Vec<String>` — port
     `pairs.py:68-73`: pairs with a `summary_rank` set, no `chunk` yet, and a non-empty
     `chunk_id`.
   - `attach_source_chunks(pairs: &mut [ChunkSummaryPair], chunks: &[SearchItem])` — port
     `pairs.py:76-80`, matching by `result_id(chunk)`.
   - `summary_text_by_chunk_id(pairs: &[ChunkSummaryPair]) -> HashMap<String, String>` — port
     `pairs.py:83-88`.
   - `summary_id_for_chunk(chunk_id: &str) -> Option<String>` — parse `chunk_id` as a `Uuid`
     (`None` on parse failure, mirroring the `try/except (TypeError, ValueError)` at
     `pairs.py:91-96`), then `Uuid::new_v5(&chunk_uuid, b"TextSummary").to_string()`. This is
     the **same scheme already implemented** in
     `crates/cognify/src/summarization/models.rs:73`
     (`let id = Uuid::new_v5(&chunk_id, b"TextSummary");` inside `TextSummary::new`) — do not
     reinvent it, just mirror the two-line computation so `pairs.rs` has no dependency on the
     `cognee-cognify` crate (would be a layering violation; `cognee-search` does not currently
     depend on `cognee-cognify`).
   - `set_pair_chunk`, `_find_chunk_summary_pair`, `_new_chunk_summary_pair` — private helpers,
     ported verbatim from `pairs.py:99-131` (linear scan over the small per-query candidate
     list; `2 * chunks_top_k` candidates at most, so this stays O(n²) but n is tiny).

4. **`ranking.rs` — port `hybrid/ranking.py` (59 lines), Phase 1 subset only.** Signature:
   `rank_chunk_summary_pairs(pairs: Vec<ChunkSummaryPair>, limit: usize, use_importance_weight: bool) -> Vec<ChunkSummaryPair>`
   (the Python function also threads `use_truth_weight`/`q_coords`/`truth_state_by_id`/
   `current_truth_epoch` for the Phase-2 truth-subspace boost — per the locked Phase-2
   deferral, do **not** implement or accept those parameters here; Phase 2's task will extend
   this signature, gated by a default-off flag, without touching the Phase-1 code path).
   - `rrf_k(chunks_top_k: usize) -> usize { (20 + 2 * chunks_top_k).clamp(30, 60) }` — port
     `_rrf_k` (`ranking.py:51-52`); note the Python `max(30, min(60, ...))` and Rust
     `.clamp(30, 60)` are equivalent for non-negative inputs.
   - `importance_factor(chunk_payload: &Value) -> f64` — port `_importance_factor`
     (`ranking.py:55-59`): `chunk_payload.get("importance_weight").and_then(Value::as_f64)`,
     default `0.5` if missing or not a JSON number (this also naturally rejects
     strings/bools/arrays, matching Python's `isinstance(x, (int, float))` guard — note
     Python's `isinstance(True, int)` is `True` in CPython, so a literal `bool` payload value
     *would* pass Python's guard; this is an intentional, documented divergence since it is
     not a realistic payload shape). Clamp to `[0.0, 1.0]`, return `0.75 + 0.5 * importance`.
   - `rank_chunk_summary_pairs`: if `limit == 0` return `vec![]`. Compute `k = rrf_k(limit)`.
     For each pair with `chunk.is_some()`: collect present ranks from
     `(bm25_rank, vector_rank, summary_rank)` in that order, skip the pair if none are set
     (`ranking.py:26-32`); `rrf_score: f64 = ranks.iter().map(|r| 1.0 / (k + r + 1) as f64).sum()`;
     `final_score = rrf_score * if use_importance_weight { importance_factor(payload) } else { 1.0 }`;
     `chunk_id_string = pair.chunk_id.clone().or_else(|| result_id(chunk)).unwrap_or_default()`.
     Collect `(final_score, rrf_score, min_rank, chunk_id_string, pair)` tuples, sort with a
     comparator equivalent to Python's `key=lambda item: (-final_score, -rrf_score, min_ranks, chunk_id)`
     — i.e. descending `final_score` (use `f64::total_cmp` per the locked decision, so `NaN`/
     `-0.0` sort deterministically), then descending `rrf_score`, then ascending `min_rank`,
     then ascending `chunk_id_string` (`Ord` on `String`) — then truncate to `limit`.

5. **`chunks.rs` — port `hybrid/chunks.py` (281 lines).** Define
   `pub(crate) struct HybridChunksResult { pub chunks: Vec<SearchItem>, pub chunk_summaries: HashMap<String, String> }`.
   - `summary_candidate_limit(chunks_top_k: usize, text_summaries_top_k: Option<usize>) -> usize`
     — port `chunks.py:106-109`: `text_summaries_top_k.unwrap_or(chunks_top_k)`. (Python's
     `None` branch clamps to `max(0, chunks_top_k)`; Rust's `usize` makes that clamp a no-op,
     so this is just `.unwrap_or(chunks_top_k)`.)
   - `search_collection(vector_db: &Arc<dyn VectorDB>, data_type: &str, field: &str, query_vector: &[f32], limit: usize, node_name: Option<&[String]>, operator: &str, required: bool) -> Result<Vec<SearchItem>, SearchError>`
     — port `chunks.py:143-175` adapted to Rust's `VectorDB` trait, which (unlike Python's
     `vector_engine.search`) has **no server-side `node_name` filter parameter**
     (`crates/vector/src/vector_db_trait.rs:67-73`). Guard with
     `vector_db.has_collection(data_type, field).await?` (same pattern as
     `crates/search/src/retrievers/chunks_retriever.rs:52-60` and
     `crates/search/src/retrievers/summaries_retriever.rs:49-53`): if missing and `required`,
     return `SearchError::NotFound(format!("missing vector collection: {data_type}_{field}"))`
     (Python raises a distinct `NoDataError`; Rust has no such variant in
     `crates/search/src/types/errors.rs`, so reuse `NotFound` — same convention the two
     existing retrievers already use — documented in Parity notes); if missing and optional,
     return `Ok(vec![])`. Otherwise: **in-retriever node filter with over-fetch** — if
     `node_name` is `Some`, call `search_similar` with an inflated `fetch_limit =
     (limit.saturating_mul(4)).max(limit + 20)` (a new, documented constant — no existing
     precedent for this in the codebase), convert to `SearchItem` via
     `search_results_to_context` (`context_items.rs:5-19`), filter with
     `payload_matches_node_filter`, then truncate to `limit`; if `node_name` is `None`, call
     `search_similar` with `limit` directly (no over-fetch/filter needed).
   - `search_bm25_chunks(graph_db: &Arc<dyn GraphDBTrait>, query: &str, limit: usize, node_name: Option<&[String]>, operator: &str) -> Vec<SearchItem>`
     — port `chunks.py:112-140`, but note the BM25 lane already landed as the free function
     `crate::retrievers::bm25_scored_chunks(graph_db, query, limit)` (P1-03). Just call it:
     `let scored = crate::retrievers::bm25_scored_chunks(graph_db, query, limit).await;`. P1-03
     already handles `limit == 0` → `[]`, the fresh-per-call corpus rebuild (decision 5; do
     **not** reuse `LexicalRetriever`'s `OnceCell` cache), the truncate-to-limit-then-drop-
     non-positive ordering, and it is fail-open (returns `Vec`, never `Err` — a corpus-load
     error is logged and degrades to an empty channel internally, matching `chunks.py:124-128`).
     So this function only *shapes* P1-03's output: map each `(payload, score)` into a
     `SearchItem` (`id`: from `result_id(&item)` parsed as `Uuid` when possible else `None`,
     `score: Some(score)`, `payload`), then apply `payload_matches_node_filter`. A defensive
     `scored_payload` clamp is redundant here (scores are already `> 0` and finite from P1-03)
     but harmless if kept. This function returns a plain `Vec<SearchItem>` (never a `Result`).
   - `load_source_chunks_for_summaries(vector_db, chunk_ids: &[String], node_name, operator) -> Result<Vec<SearchItem>, SearchError>`
     — port `chunks.py:178-209` using the P1-02 retrieve-by-ids call
     (`VectorDB::retrieve("DocumentChunk", "text", &uuids)`), log missing ids (present in
     `chunk_ids` but absent from the fetched set) at `warn`, then filter the fetched chunks
     through `payload_matches_node_filter`, logging (at `warn`) any that fail the filter
     (`chunks.py:204-208`) — those are dropped, not returned.
   - `load_summary_text_for_ranked_pairs(vector_db, ranked_pairs: &mut [ChunkSummaryPair], node_name, operator) -> Result<(), SearchError>`
     — port `chunks.py:212-280`: for pairs missing `summary_text` but carrying a `chunk_id`,
     resolve `summary_id` (prefer the existing `pair.summary_id`, else
     `summary_id_for_chunk(chunk_id)`; skip with a `debug` log if the chunk id isn't a valid
     UUID). Batch-fetch via `VectorDB::retrieve("TextSummary", "text", &summary_uuids)`
     (return early/`Ok(())` on a "collection not found" condition — Rust: check
     `has_collection` first rather than pattern-matching an error variant, consistent with
     step 5's `search_collection` guard style); for each summary found, extract `text` via
     `display_value`, apply `payload_matches_node_filter`, and only then write
     `pair.summary_text` — log (`warn`) the three failure cases Python logs (no paired
     summary, empty summary text, filter failure) at `chunks.py:255-278`.
   - `retrieve_hybrid_chunks(vector_db: &Arc<dyn VectorDB>, graph_db: &Arc<dyn GraphDBTrait>, query: &str, chunks_top_k: usize, text_summaries_top_k: Option<usize>, node_name: Option<&[String]>, node_name_filter_operator: &str, use_importance_weight: bool, query_vector: &[f32]) -> Result<HybridChunksResult, SearchError>`
     — port `chunks.py:27-103` (Phase-1 subset — omit the `query_vector: Optional` branch:
     this task **receives** the already-computed `query_vector` as a parameter (the future
     retriever task, P1-09, embeds the query once and passes it down), so there is no embedding
     call inside `chunks.rs`; also omit the
     `use_truth_weight`/`q_coords`/`truth_state_by_id`/`current_truth_epoch` parameters per
     the Phase-2 deferral in step 4). `candidate_limit = chunks_top_k * 2`; `summary_limit =
     summary_candidate_limit(...)`. Run the three lanes concurrently with
     `tokio::try_join!(bm25_future, vector_future, summary_future)` (BM25 lane never returns
     `Err`, so wrap it as `Ok(...)` inside the joined future, or use `tokio::join!` for the
     BM25 leg and `try_join!`-equivalent handling for the two fallible vector legs — either
     is acceptable as long as a `required=true` `NotFound` from the vector lane still
     propagates as an `Err` from `retrieve_hybrid_chunks`). Then: build pairs, backfill source
     chunks for summary-only pairs, rank, backfill summary text for the ranked pairs, and
     return `HybridChunksResult { chunks: ranked_pairs.into_iter().filter_map(|p| p.chunk).collect(), chunk_summaries: summary_text_by_chunk_id(&ranked_pairs) }` — note `summary_text_by_chunk_id` must be called on the **ranked** pairs (post-backfill), matching `chunks.py:100-103`.

6. **Wire the two `Cargo.toml` feature additions** (`crates/search/Cargo.toml`): add `"v5"` to
   the `uuid` dependency's feature list (needed by `pairs::summary_id_for_chunk`), and add
   `"macros"` to the `tokio` dependency's feature list (needed by `tokio::try_join!` /
   `tokio::join!` in `chunks::retrieve_hybrid_chunks`; currently only `"sync"` is enabled at
   line 35, and the dev-dependency `tokio` override with `"macros"` only applies to
   `#[cfg(test)]` builds, not the library target).

7. Run `cargo check --all-targets -p cognee-search`, then `cargo test -p cognee-search`, then
   the project-wide `scripts/check_all.sh` once all four files compile and unit tests pass.

## Parity notes

- **BM25 constants** (owned by P1-03, consumed here): `k1 = 1.5`, `b = 0.75`,
  `idf(token) = ln(1 + (N - df + 0.5) / (df + 0.5))`, score summed over **unique** query
  terms only (`bm25_retriever.py:74-105`). This task does not reimplement BM25; it only
  filters/shapes its output.
- **RRF**: `rrf_k = clamp(20 + 2*chunks_top_k, 30, 60)` (`ranking.py:51-52`); `rrf_score =
  Σ 1/(rrf_k + rank + 1)` over the 0-based ranks present for a chunk in `{bm25, vector,
  summary}` (`ranking.py:34`, `pairs.py:24-42,44-63` for where ranks are assigned).
- **Importance factor**: `0.75 + 0.5 * clamp(importance_weight, 0, 1)`, default `0.5` when
  the payload's `importance_weight` is missing or non-numeric (`ranking.py:55-59`).
- **Sort key**: `(-final_score, -rrf_score, min(ranks), chunk_id_string)` ascending, i.e.
  final_score desc → rrf_score desc → min-rank asc → chunk_id string asc
  (`ranking.py:47`). Use `f64::total_cmp` for the two float comparisons (workspace convention
  for total ordering over floats containing possible `NaN`/`-0.0`).
- **`summary_id_for_chunk` id scheme**: `uuid5(chunk_uuid, "TextSummary")` —
  `pairs.py:91-96` — already implemented identically in
  `crates/cognify/src/summarization/models.rs:73` as `Uuid::new_v5(&chunk_id, b"TextSummary")`.
  `pairs.rs` must compute the exact same two-liner locally (no cross-crate call — see step 3)
  so the two never drift.
- **`belongs_to_set` shape divergence** (Rust vs Python): Python stores plain dataset-id
  strings; Rust's `classify_documents` (`crates/models/src/document.rs:148-178`) stores
  `NodeSet` **objects** `{"id", "name", "type": "NodeSet"}` for document-level node sets,
  while `DataPoint::new`'s dataset-membership entries remain plain strings
  (`crates/models/src/data_point.rs`). `payload_matches_node_filter` must read `.name` off
  object entries and tolerate bare-string entries — this is called out explicitly in the
  brief and is **not** a bug to fix, just a shape this port must handle.
  `node_name_filter_operator`: only the literal `"AND"` selects AND semantics; anything else
  (including case variants or garbage) is OR — no validation error, matching Python exactly
  (unlike the stricter `AND`/`OR`-only validation in
  `crates/search/src/graph_retrieval/brute_force_triplet_search.rs:126-130`, which this port
  intentionally does **not** copy).
- **`NoDataError` → `SearchError::NotFound`**: Python raises a dedicated
  `cognee.modules.retrieval.exceptions.exceptions.NoDataError` when the required
  `DocumentChunk_text` collection is missing (`chunks.py:170-173`). Rust's
  `crates/search/src/types/errors.rs` has no equivalent variant; this task reuses
  `SearchError::NotFound`, matching the existing convention in `ChunksRetriever` and
  `SummariesRetriever`. If a later task wants byte-for-byte error-type parity with the Python
  SDK's exception hierarchy, that is a separate, explicit decision — not silently introduced
  here.
- **Node-name filter push-down (finding F9 — resolved server-side)**: Python's
  `vector_engine.search(..., node_name=..., node_name_filter_operator=...)` filters
  server-side inside the vector adapter (filter-then-limit). Rust now mirrors this: the trait
  gained `VectorDB::search_similar_filtered(data_type, field, query_vector, top_k, node_name,
  operator)` and `search_collection` threads `node_name`/`operator` into it instead of
  over-fetching and filtering client-side. The membership predicate
  (`crates/vector/src/node_filter.rs::metadata_matches_node_filter`) mirrors
  `payload_matches_node_filter` byte-for-byte (object `.name` match, bare-string entries do
  not match NodeSet names, `"AND"`=subset else OR-intersection). Recall by backend:
  `BruteForceVectorDB` / `MockVectorDB` filter during the in-memory scan and pgvector pushes
  the membership into the JSONB `WHERE`, so all three are **exact filter-then-limit at any
  collection size**. Only the LanceDB / default-trait fallback (whose `metadata` is an opaque
  JSON string it cannot predicate on) keeps a bounded client-side limit-then-filter that
  widens to the whole collection under `NODE_FILTER_RECALL_FETCH_CAP` (4096) — exact at or
  below the cap, approximate only above it. When `node_name` is `None`/empty the call is
  byte-identical to the previous plain `search_similar` path.
- **BM25 lane never hard-fails the search**: a BM25 lane error is logged and treated as an
  empty channel (`chunks.py:124-128`); only the *required* `DocumentChunk_text` vector lane
  can turn a missing collection into a hard error.
- **Truth-subspace (Phase 2) params are intentionally absent** from `ranking.rs`'s and
  `chunks.rs`'s Phase-1 signatures — see step 4/5. Do not add unused parameters "for future
  use" in this task; Phase 2's task will extend the signatures when it lands.

## Testing

- `results.rs` unit tests: `display_value` on each `Value` variant (string/number/bool/null/
  array/object, including whitespace-only string → `None`); `payload_matches_node_filter` with
  (a) a `belongs_to_set` of bare-string entries, (b) `NodeSet`-object entries, (c) a mix, (d)
  `AND` requiring a superset match, (e) `OR` requiring any overlap, (f) no `belongs_to_set`
  key → `false` when `node_name` is `Some`, (g) `node_name: None` → always `true`; `result_id`
  falling back from payload `id` to `SearchItem.id`.
- `pairs.rs` unit tests: two BM25+vector hits for the same id merge into one pair with both
  ranks set; a BM25 hit with no id text-merges onto a same-text, id-less pair and later
  adopts an id from a matching vector hit (`pairs.py:37-40` case); a summary hit with no
  `source_chunk_id` is dropped with no pair created; `summary_id_for_chunk` round-trips
  against `TextSummary::new`'s id for a few sample chunk ids (cross-check with
  `cognee_cognify::summarization::models::TextSummary::new` in an integration test, or inline
  the same `Uuid::new_v5` call and assert equality — avoid a `cognee-cognify` dev-dependency
  if it would create a cycle; check first).
- `ranking.rs` unit tests: `rrf_k` boundary values (`chunks_top_k=0` → `30`; `chunks_top_k=5`
  → `30`; `chunks_top_k=20` → `60`; `chunks_top_k=100` → `60`); a chunk present in all three
  lanes outranks one present in only one; `importance_factor` for missing/non-numeric/`0`/`1`/
  out-of-range (`1.5`, `-1`) payload values; sort-key tiebreak by `chunk_id` when two pairs
  tie on `final_score`/`rrf_score`/`min(ranks)`; `limit=0` returns `[]`.
- `chunks.rs` tests using `MockVectorDB` (via `cognee-test-utils`, already a dev-dependency
  pattern in this crate) plus a `MockGraphDB` populated with `DocumentChunk` nodes to drive
  the BM25 lane (there is no BM25 object to stub — `bm25_scored_chunks` reads directly from
  the passed `graph_db`): (a) missing `DocumentChunk_text`
  collection → `Err(SearchError::NotFound(_))`; (b) missing `TextSummary_text` collection is
  tolerated (empty summary channel, not an error); (c) `node_name` filtering drops
  non-matching vector hits via the over-fetch path; (d) a summary-only hit triggers a
  source-chunk backfill via retrieve-by-ids; (e) `chunk_summaries` map is populated for the
  final ranked pairs, not the pre-rank candidate set.
- Add these as inline `#[cfg(test)] mod tests` blocks in each file, matching this crate's
  existing convention (`lexical_retriever.rs`, `chunks_retriever.rs`).

## Checkpoints / acceptance

- [x] `crates/search/src/retrievers/hybrid/{mod,results,pairs,ranking,chunks}.rs` exist and
      compile.
- [x] `cargo check --all-targets` passes for the workspace.
- [x] `cargo test -p cognee-search` passes, including all new unit tests listed above.
- [x] `scripts/check_all.sh` passes (fmt, clippy `-D warnings`, C/Python/TS/Java binding
      checks unaffected since no public API surface changed).
- [x] No `unwrap()` in non-test code in any of the four new files; any `expect()` carries a
      why-comment; fallible paths use `?`/`map_err`.
- [x] `rank_chunk_summary_pairs`'s constants (`rrf_k` formula, importance-factor formula, sort
      key) match `ranking.py` exactly — spot-check with a unit test that reproduces a small
      hand-computed example.
- [x] No dependency added from `crates/search` on `crates/cognify` (the `TextSummary` id
      scheme is duplicated inline per step 3/Parity notes, not imported).

## Risks & open items

- **P1-02/P1-03/P1-04/P1-05 interface drift — RESOLVED (all landed; signatures re-verified in
  Prerequisites).** The one material drift vs the original draft: **P1-03 landed as a free
  function `bm25_scored_chunks(graph_db: &Arc<dyn GraphDBTrait>, query, limit) -> Vec<(Value,
  f32)>`, not a `BM25ChunksRetriever` struct**, so this task threads a
  `graph_db: &Arc<dyn GraphDBTrait>` (not a BM25 object) through `search_bm25_chunks` and
  `retrieve_hybrid_chunks` and calls the function directly — see the corrected step 5
  signatures. `VectorDB::retrieve` landed exactly as assumed. `importance_weight` landed on
  `DataPoint` (P1-04), not `DocumentChunk`; `ranking.rs`'s defensive JSON read is unaffected.
- **Over-fetch multiplier is a guess** (`(limit*4).max(limit+20)`) with no existing
  precedent in the codebase and no tuning data yet; revisit once cross-SDK parity runs
  surface real under/over-fetch behavior on node-scoped queries.
- **`SearchError::NotFound` vs a dedicated `NoData` variant**: reusing `NotFound` matches
  existing convention but is a deliberate, low-risk divergence from Python's distinct
  exception type — flag if a later task wants richer error taxonomy on the wire.
- **`hybrid` module is `pub(crate)`-only for now**: no `SearchType::HybridCompletion`, no
  `SearchRetriever` impl, no `SearchParams` knobs are added by this task (those are locked
  for a later task per the wire-exposure decision). This task is inert from the outside —
  acceptance here is purely "the module compiles, is unit-tested, and is ready to be called."
- **Concurrency primitive choice**: `tokio::try_join!` requires the `"macros"` feature on the
  library target (not just dev-dependencies); confirm this doesn't pull in an unwanted
  `#[tokio::main]`-style proc-macro surface for a library crate — it does not (the macro
  feature also gates `join!`/`try_join!`/`select!`, which are the only ones used here).
