# P1-12 — Documentation updates

**Status:** completed

## Refiner corrections (Agent 1) — READ FIRST, these OVERRIDE the original steps below

Verified against committed code on `feat/hybrid-retriever-port` (P1-01..P1-11 all
landed). The original "Implementation steps" were written before P1-01/P1-09
landed and contain several now-wrong assumptions. Apply the corrections here;
treat the numbered steps below as reference only where they conflict.

### C-0. Facts established by reading the code (authoritative)

- **Core `SearchType` enum = 16 variants** (`crates/search/src/types/search_type.rs`),
  including `Feedback`. **Wire `WireSearchType` = 15** (Feedback dropped).
- **Hybrid knobs are NOT wire fields.** `SearchPayloadDTO`
  (`crates/http-server/src/dto/search.rs`) has **no** hybrid knob fields and **no**
  `retriever_specific_config` field — its only fields are `search_type`, `datasets`,
  `dataset_ids`, `query`, `system_prompt`, `node_name`, `top_k`, `only_context`,
  `verbose`. The hybrid knobs are typed fields on **`SearchParams`**
  (`crates/search/src/types/search_params.rs`), populated in `impl From<&SearchRequest>`
  from `SearchRequest.retriever_specific_config` (a JSON map). **Every external
  adapter hardcodes `retriever_specific_config: None`** — `http-server/src/routers/search.rs:189`,
  `http-server/src/responses_dispatch.rs:211`, `cli/src/commands/search.rs:117`,
  `bindings-common/src/ops/retrieval.rs:156`. So in Phase 1 the hybrid knobs are
  reachable **only by a direct Rust library caller**, exactly like the other
  orchestrator-only knobs (`neighborhood_depth`, etc.) — this is the pre-existing
  wire-passthrough gap noted in search.md open question 4, NOT a new wire surface.
  → The step-3 / step-5 framing ("first-class wire fields on `SearchPayloadDTO`",
  "expose every hybrid knob on POST /api/v1/search from day one", camelCase wire
  columns) is **FALSE — do not document it that way.**
- **`SearchParams` hybrid field names (exact, committed):** `chunks_top_k`,
  `entities_top_k`, `facts_top_k`, `max_edges_per_entity`, `text_summaries_top_k`,
  `use_importance_weight`, `use_truth_weight`, `include_global_context_index`,
  `global_context_index_top_k` (all `Option<...>`), plus the reused `node_name` /
  `node_name_filter_operator`. `retriever_specific_config` JSON keys are the
  **snake_case** names (see the `hybrid_knobs_extracted_from_retriever_specific_config`
  test in search_params.rs).
- **Actual Rust defaults (from the `HybridRetriever::new` constructor,
  `crates/search/src/retrievers/hybrid/mod.rs`), NOT the Python `__init__` signature:**
  `DEFAULT_TOP_K = 15`, `DEFAULT_MAX_EDGES_PER_ENTITY = 10`,
  `DEFAULT_GLOBAL_CONTEXT_INDEX_TOP_K = 3`, `DEFAULT_NODE_NAME_FILTER_OPERATOR = "OR"`.
  Resolution at retrieve-time is three-layer: `params.<knob>` → `params.top_k` →
  constructor default (mod.rs:361-379). So the effective defaults are:
  `chunks_top_k`/`entities_top_k`/`facts_top_k` = **`top_k`, falling back to 15**
  when `top_k` is also unset (Python parity; note the HTTP DTO's own `top_k`
  default is 10); `max_edges_per_entity` = **10**; `text_summaries_top_k` =
  **`None`** (no fallback); `use_importance_weight` = **`true`**; `node_name` =
  **`None`**; `node_name_filter_operator` = **`"OR"`**. Reserved:
  `use_truth_weight` = **`false`**, `include_global_context_index` = **`false`**,
  `global_context_index_top_k` = **3**. (NB: the `SearchParams` doc-comments say
  "unwrap_or(10)" for chunks/entities/facts — that is a stale code comment; the
  constructor uses `DEFAULT_TOP_K = 15`. Document 15; flag the comment nit to the
  phase lead, do not fix it in this docs task.)
- **`get_neighborhood` confirmed present** on `GraphDBTrait`
  (`crates/graph/src/traits.rs:517`) and impls `ladybug.rs:1442`,
  `pg_graph_adapter.rs:1269`, `mock.rs:488`, with direction-preserving tests in
  `mock.rs:814` (`get_neighborhood_preserves_edge_direction`). The architecture.md
  step-1 note about it is valid (adjust the cited line refs — ladybug bidirectional
  test is not at `ladybug.rs:2040` in the current tree; use the mock direction test
  or re-grep).

### C-1. Resolved "8 active + 2 reserved" knob accounting

- **8 active knobs:** `chunks_top_k`, `entities_top_k`, `facts_top_k`,
  `max_edges_per_entity`, `text_summaries_top_k`, `use_importance_weight`,
  `node_name`, `node_name_filter_operator`.
- **2 reserved (Phase-2, inert) groups = 3 fields:** (1) `use_truth_weight`;
  (2) global-context = `include_global_context_index` + `global_context_index_top_k`.
  The `HybridRetriever` doc-comment (mod.rs:84-88) and the `SearchParams` field
  comments already mark these three "reserved/inert — Phase 2". `build_truth_subspace`
  on `improve()` is Python-side only; keep it as a cross-reference note, not a
  documented Rust knob.

### C-2. What P1-01 ALREADY did — DO NOT redo

- `docs/operations.md:93` — already reads **"16 retrieval strategies"** and the
  list already contains `HybridCompletion`. **Only remaining op-doc edit:** delete
  the stale parenthetical **"(not yet backed by a retriever)"** after
  `HybridCompletion` (operations.md ~line 98) and replace with the one-line gloss
  from step 2. Do NOT touch the count.
- `docs/http-server/routers/search.md` wire counts are already at 15:
  line 76 ("one of the 15 wire variants"), line ~205 ("exposes **15** variants"),
  line ~225 ("**9 of the 15**" + "remaining 6" list already includes
  `HybridCompletion`). **Do NOT re-bump these.**
- The §4 wire-table **`HYBRID_COMPLETION` row already EXISTS** (added by P1-01 as
  interim). **Agent 2 must UPDATE it, not add a new row:** change its status/notes
  from *"**Not implemented (interim)** | Registered by P1-09; until then returns
  `SearchError::UnsupportedSearchType`."* to **Implemented** + the real description
  (BM25 lexical + vector chunks/entities/edge-facts + 1-hop graph-neighborhood
  expansion, LLM completion; `search_result` is a `String`).
- search.md line ~225 parenthetical *"(`HybridCompletion` has neither yet — it is
  not backed by a retriever until P1-09)"* is now **stale** → update: it is backed,
  has unit + integration coverage, still lacks cross-SDK E2E (Phase 2 scope).

### C-3. Corrected file/scope list for Agent 2

**In scope — the 5 planned doc files:**

1. `docs/architecture.md` — line 77 "15 search modes" → **16** + hybrid clause;
   cognee-graph bullet: add `get_neighborhood(ids, depth)` note (fix line refs per
   C-0); rustdoc table Search row: append `HybridRetriever`.
2. `docs/operations.md` — count already done; only drop "(not yet backed by a
   retriever)" and add the gloss (see C-2).
3. `docs/configuration.md` — add the hybrid-knob section, **reframed** per C-0:
   these are **`SearchParams` (library/orchestrator) knobs**, supplied as
   **snake_case keys inside `SearchRequest.retriever_specific_config`**, NOT wire
   DTO fields. Table columns: `SearchParams` field | `retriever_specific_config`
   JSON key (snake_case) | Effective Rust default | Python parity. Use the
   **actual defaults from C-0** (top_k→15, not "5"). Add the "Reserved (Phase 2,
   inert)" sub-list (C-1). State clearly that in Phase 1 these knobs are **not
   reachable via HTTP/CLI/bindings** (retriever_specific_config hardcoded `None`),
   only via a direct library `SearchRequest` — same class as `neighborhood_depth`
   (cross-link search.md open question 4). Drop the "first-class wire field" framing.
4. `docs/tools/cli.md` — pointer sentence to the canonical enum list + the
   `HYBRID_COMPLETION` example line (step 4 is fine as written).
5. `docs/http-server/routers/search.md` —
   - line 3 prose *"one of **fifteen** `SearchType` strategies"* → **"sixteen"**
     (this line tracks the **core enum** count, as it did at init when it said
     "fifteen" while the wire was 14; the wire-specific counts at 76/205/225 stay
     15 and remain correct — the 16-core / 15-wire split is intentional).
   - UPDATE (not add) the existing `HYBRID_COMPLETION` wire-table row (C-2).
   - update the stale line-225 parenthetical (C-2).
   - §4 `SearchOutput` block: add `HybridCompletion` to the `Text(String)` comment.
   - **DO NOT add 8 fields to the `SearchPayloadDTO` code block** — they do not
     exist in the real struct; the code block must keep mirroring the real DTO.
     Instead add a short note under the DTO block that `HYBRID_COMPLETION`'s tuning
     knobs are `SearchParams`/`retriever_specific_config` and are not wire-exposed
     in Phase 1 (point to configuration.md §Search and open question 4).
   - Add a **"Known limitations — `HYBRID_COMPLETION`"** section (3 bullets from
     step 5, but reworded: `includeGlobalContextIndex` is a
     `retriever_specific_config` key that is accepted-and-inert, NOT a
     "wire field that round-trips"). **Section numbering:** current headings run
     §1..§7 with §7 = References. Insert **"## 7. Known limitations —
     `HYBRID_COMPLETION`"** between the current §6 Open questions and References,
     and renumber References **§7 → §8**. (Ignore the original step-5's confused
     "§8/§9" scheme.)
   - References: add the new Python file links (step 5 list is fine).

**In scope — stray-count sweep deferred from P1-01 (per pipeline brief):**
all describe the crate/binding surface = the **core 16** enum, currently stale at 15:

6. `crates/search/src/lib.rs:1` — `//!` "15 retrieval strategies" → **16** (doc
   comment only, no logic).
7. `crates/search/README.md:3` — "15 retrieval strategies" → **16**.
8. `python/README.md:217` — "All 15 search types are supported" → **16** (the
   Python `SearchType` enum in `python/cognee_py/__init__.py` lists all 16
   incl. `FEEDBACK` + `HYBRID_COMPLETION`).
9. `python/src/sdk_retrieval.rs:86` — "one of the 15 search strategy strings" →
   **16** (doc comment only).
10. `python/cognee_py/types.py:104` — "One of the 15 `SearchType` strings" → **16**
    (and re-grep `python/cognee_py/__init__.pyi` for any matching "15" comment).
11. `ts/README.md:161` — "All 15 search types are supported" → **16**.

**Already correct — DO NOT touch:** `ts/src/types.ts:101` + `ts/lib/types.d.ts:95`
("All 16 …"), `capi/examples/sdk_retrieval_smoke.c:236` ("all 16 …"),
`capi/include/cognee_sdk.h` list, `java/.../SearchType.java` (16 members, no count
comment), `docs/http-server/routers/README.md:38` ("the 15 search types" = wire,
correct), `docs/http-server/routers/recall.md:3,189` ("15 `SearchType` values" =
wire-reachable set, correct — and recall.md is out of scope per Risks).

**Out of scope / no stale count found:** root `README.md` (no numeric search-type
count exists), `docs/http-server/routers/recall.md` (see Risks; its 15s are correct
wire counts anyway).

### C-4. Final grep gate (replaces Testing §1)

After edits, this must return **zero** stale hits (adjust for the intentional
wire-15 survivors):
`grep -rniE "15 (search modes|retrieval strateg)|fifteen .SearchType|9 of the 14|14 .*variant" docs/ crates/search/README.md crates/search/src/lib.rs python/README.md python/src/sdk_retrieval.rs python/cognee_py/types.py ts/README.md`

## Goal

Bring the five documentation surfaces that describe the search subsystem
current with the `HYBRID_COMPLETION` port: the crate/architecture inventory,
the operations overview, the configuration reference, the CLI reference, and
the HTTP `search` router spec. This is the doc-only closeout task for Phase 1
of the hybrid-retriever port — per `.claude/CLAUDE.md`'s "keep docs current"
mandate, no other task in this phase touches these five files, so without this
task they drift the moment `SearchType::HybridCompletion` ships. It also
carries the one required "Known limitations" note so downstream integrators
(HTTP clients, other SDK ports) know exactly which knobs are wire-accepted but
functionally inert or degraded in Phase 1.

## Prerequisites

- **P1-01** — lands the core `HybridRetriever` port: the `SearchType::HybridCompletion`
  variant, the per-query BM25 lane (Okapi BM25, not the existing Jaccard
  `LexicalRetriever`), the entities/facts lanes, and the `GraphDBTrait::get_neighborhood(ids, depth)`
  batched addition (~4 backend impls: `LadybugAdapter`, `PgGraphAdapter`,
  `MockGraphDB`, plus whichever formatted/wrapper impl `crates/graph/src/formatted.rs`
  turns out to need). This task assumes those symbols exist and compile before
  documenting them.
- **P1-09** — lands the HTTP wire surface: the `HYBRID_COMPLETION` row on
  `WireSearchType`, the new hybrid knobs on `SearchPayloadDTO` (or a nested
  struct), and the request→`SearchParams` plumbing. This task assumes the
  field names P1-09 actually lands with; the field list proposed below (taken
  from the Python constructor signature) is the expected shape, but **before
  editing `docs/http-server/routers/search.md`, diff it against P1-09's landed
  DTO** and correct any renamed field before publishing.
- **Assumed state**: `cargo check --all-targets` and `scripts/check_all.sh`
  are green on top of P1-01 + P1-09 (this task adds no code, so it cannot fix
  a red build — it only documents what's already merged).

## Files touched

- `docs/architecture.md` (edit) — crate breakdown (`cognee-search`, `cognee-graph`
  bullets) and the rustdoc quick-reference table.
- `docs/operations.md` (edit) — the `search (retrieval)` subsection's strategy
  count and list.
- `docs/configuration.md` (edit) — new "Search — hybrid retriever knobs"
  section.
- `docs/tools/cli.md` (edit) — `search` subcommand row + example.
- `docs/http-server/routers/search.md` (edit) — new wire-type table row,
  `SearchOutput` mapping comment, new DTO fields, a new "Known limitations"
  section, and the E2E-coverage count.

All five paths were opened and verified to exist with the line numbers cited
below as of this writing (commit `82310dc`); re-verify line numbers at
execution time since P1-01/P1-09 will have landed unrelated edits to some of
these files in between (`http-server/routers/search.md` is the most likely to
have moved).

## Implementation steps

1. **`docs/architecture.md`**
   - Line 77 (`cognee-search` bullet): change *"`SearchType` enum defines 15
     search modes"* → *"16 search modes"* and append a clause naming the new
     retriever, e.g. `; the newest, HybridCompletion, blends BM25 lexical +
     vector + graph-neighborhood entity/fact channels`.
   - Line 85 (`cognee-graph` bullet, `GraphDBTrait` description): add a clause
     noting the batched `get_neighborhood(ids, depth) -> (nodes, edges)` method
     and that it returns the **true stored edge direction** (fixing the
     previous undirected direction-flip that `get_connections` has — see
     `crates/graph/src/traits.rs:168` and the bidirectional test at
     `crates/graph/src/ladybug.rs:2040`).
   - Line 176 (rustdoc quick-reference table, Search row): append
     `HybridRetriever` to the "Start at" cell: `` `SearchBuilder`, `SearchType`, `HybridRetriever` ``.
   - Do not touch the "Key dependencies" table — BM25 uses no new crate
     dependency (pure-Rust port of the Python scorer/tokenizer, no `bm25`-named
     crate).

2. **`docs/operations.md`**
   - Lines 93–99 (search subsection): bump *"15 retrieval strategies"* →
     *"16 retrieval strategies"* and insert `HybridCompletion` into the
     comma-separated list, ideally right after `GraphSummaryCompletion` (it is
     the other multi-channel completion retriever) and before `TripletCompletion`.
     One-line gloss to add inline or as a following sentence: *"`HybridCompletion`
     combines a per-query BM25 lexical pass over chunks, vector search over
     chunks/entities/edge-facts, and 1-hop graph-neighborhood expansion around
     matched entities, then answers via LLM completion."*
   - No change needed to the "Operation → interface map" table (§ operation
     rows are per-verb, not per-`SearchType`).

3. **`docs/configuration.md`**
   - Insert a new `## Search — hybrid retriever knobs` section. Placement:
     after `## Chunking & tokenizer` (line 163) and before `## Ontology`
     (line 164), matching the doc's subsystem-grouping order.
   - Open with a one-sentence framing note explaining why this breaks the
     page's usual "env-var only" convention: unlike other retrievers' tuning
     knobs (`neighborhood_depth`, `wide_search_top_k`, `triplet_distance_penalty`,
     …), which are library/orchestrator-only (`SearchParams`, no env binding,
     not all reach the HTTP DTO — see `docs/http-server/routers/search.md`
     open question 4), the hybrid knobs are deliberately first-class **wire**
     fields on `SearchPayloadDTO` per the phase-1 design decision to expose
     every hybrid knob on `POST /api/v1/search` from day one.
   - Document the knob table (verify final field names against the P1-09 DTO
     before publishing — names below are the expected 1:1 snake_case port of
     the Python `HybridRetriever.__init__` signature,
     `/tmp/cognee-python/cognee/modules/retrieval/hybrid_retriever.py:36-54`):

     | `SearchParams` field | Wire field (camelCase) | Default | Python default |
     |---|---|---|---|
     | `chunks_top_k` | `chunksTopK` | `5` | `chunks_top_k=5` |
     | `entities_top_k` | `entitiesTopK` | `5` | `entities_top_k=5` |
     | `max_edges_per_entity` | `maxEdgesPerEntity` | `10` | `max_edges_per_entity=10` |
     | `text_summaries_top_k` | `textSummariesTopK` | `None` (derives from `chunks_top_k`) | `text_summaries_top_k=None` |
     | `use_importance_weight` | `useImportanceWeight` | `true` | `use_importance_weight=True` |
     | `facts_top_k` | `factsTopK` | `5` | `facts_top_k=5` |
     | `include_global_context_index` | `includeGlobalContextIndex` | `false` | `include_global_context_index=False` |
     | `global_context_index_top_k` | `globalContextIndexTopK` | `3` | `global_context_index_top_k=3` |

     `node_name`, `node_name_filter_operator`, `system_prompt`,
     `system_prompt_path`, `session_id` are reused as-is from the existing
     `SearchPayloadDTO` fields (no new wire field needed).
   - Add a **"Reserved (Phase 2, inert)"** sub-list documenting the two keys
     from locked decision 4, both accepted on the wire but functionally
     no-ops until Phase 2 lands real truth-subspace execution:
     - `useTruthWeight` (`use_truth_weight`, search-time, default `false`) on
       `POST /api/v1/search` with `searchType=HYBRID_COMPLETION`.
     - `buildTruthSubspace` (`build_truth_subspace`, default `false`) on
       `POST /api/v1/improve` — out of scope for this task's file list (it's
       an `improve()`-side flag, Python: `cognee/api/v1/improve/improve.py:42`),
       cross-referenced here only so both halves of the Phase-2 seam are
       discoverable from one place.
     - State explicitly: passing `true` for either key in Phase 1 must behave
       **identically** to `false` — no partial/best-effort truth-subspace
       lookup — and link to the Known Limitations section added in step 5.
   - Cross-link this new section from the existing `## HTTP server` section
     (line 238) with one added sentence pointing at
     `http-server/routers/search.md`.

4. **`docs/tools/cli.md`**
   - Line 29 (`search` subcommand row): the `-t/--query-type` cell currently
     shows only the default (`GRAPH_COMPLETION`); leave the cell as-is (it
     documents the default, not the full enum) but add one sentence below the
     table noting the full set of accepted `--query-type` values lives in
     `docs/http-server/routers/search.md` (avoid a second place to keep the
     15/16-way enum in sync).
   - After line 79's example, add one more example line demonstrating the new
     type: `cognee-cli search "what did we learn about X?" -t HYBRID_COMPLETION -d my_dataset -k 10`.

5. **`docs/http-server/routers/search.md`** (the bulk of this task)
   - §2.2 SearchType wire table (lines 205–224): change the intro sentence's
     *"exposes **14** variants"* → *"**15** variants"* and add a new row,
     placed after the `GRAPH_SUMMARY_COMPLETION` row (line 212) to keep the
     completion-style retrievers grouped:

     ```
     | `"HYBRID_COMPLETION"` | `HybridCompletion` | `HYBRID_COMPLETION` | Implemented | BM25 lexical + vector (chunks/entities/edge-facts) + 1-hop graph-neighborhood expansion, LLM completion. `search_result` is a `String`. New wire knobs — see configuration.md §Search. |
     ```

   - Line 225: bump *"9 of the 14"* → *"9 of the 15"*, and append
     `HybridCompletion` to the "remaining 5" list, making it "remaining 6"
     (`Cypher, NaturalLanguage, FeelingLucky, CodingRules, ChunksLexical, HybridCompletion`).
     Cross-SDK parity coverage for `HybridCompletion` is explicitly Phase 2+
     scope (locked decision 2: structural-parity acceptance via the
     `e2e-cross-sdk` harness, not this doc task).
   - §4 `SearchOutput` enum block (lines 232–241): add `HybridCompletion` to
     the `Text(String)` variant's trailing comment (line 235), next to
     `RAG_COMPLETION, FEELING_LUCKY`.
   - §4 DTO code block (`SearchPayloadDTO`, lines 122–160): add the 8 new
     `Option<...>` fields from the configuration.md table in step 3, each with
     a `#[serde(default, alias = "...")]` matching the existing style (e.g.
     `chunks_top_k` snake_case alias for the camelCase `chunksTopK` field), plus
     the two Phase-2-reserved fields (`use_truth_weight`, default `false`).
     Add one doc-comment line per field pointing at the Python constructor
     parameter it mirrors.
   - Add a new numbered section **"8. Known limitations — `HYBRID_COMPLETION`"**
     (renumber the current `## 7. References` to `## 9. References`, and
     insert this as `## 8.`, i.e. move the current implementation-tasks
     section `## 5` stays, add the new section between the current `## 6. Open
     questions` and `## 7. References`). Content — three bullets, each with
     exact Python citations (see Parity notes below for the citations to
     reuse verbatim):
     1. **Entity `belongs_to_set` backfill deferred** — node-scoped
        (`nodeName` filter) `HYBRID_COMPLETION` entity search returns `[]` for
        any existing dataset whose `Entity` vector rows predate this feature,
        because the node-set filter treats a missing/non-list `belongs_to_set`
        payload field as "does not match" rather than "unscoped". No backfill
        migration ships in Phase 1 (locked decision 7) — this is a documented
        degradation, not a bug.
     2. **`includeGlobalContextIndex` unsupported** — the wire field is
        accepted (schema-valid, round-trips) but has no effect in Phase 1: the
        Rust port does not yet have a `GlobalContextSummary` node type or the
        `load_root_text` / `search_top_global_context_summaries` global-context
        utilities the Python global-context lane depends on. Setting it to
        `true` must not error — it silently yields the same context as `false`.
     3. **Existing-dataset degradation on `importance_weight` and
        `TextSummary.source_chunk_id`** — datasets cognified before this
        feature landed have `Data.importance_weight = None` (no `DataPoint`-level
        field existed yet), which the ranking lane must treat as neutral
        (`0.5`), not as a hard error or a zero. Separately, `TextSummary` rows
        written before this feature lack `source_chunk_id`, so the
        summary-**rank** channel drops them (they never enter
        `chunk_summary_pairs`'s summary-scoring path); however the chunk that
        the summary was made from can still recover the summary **text** via
        the `uuid5(chunk_id, "TextSummary")` id-derivation fallback once that
        chunk ranks by BM25/vector alone — so old datasets see reduced ranking
        signal, not missing summaries. No backfill migration ships for this
        either (locked decision 7).
   - §7 (old)/§9 (new) References: add the new Python file links used above
     (`hybrid_retriever.py`, `hybrid/pairs.py`, `hybrid/chunks.py`,
     `hybrid/results.py`, `bm25_retriever.py`, `utils/stop_words.py`,
     `utils/global_context.py`) alongside the existing entries.

## Parity notes

Exact constants/formulas/citations to carry into the docs verbatim (already
locked decisions for the implementation tasks; this task only needs to *cite*
them correctly, not re-derive them):

- **BM25**: Okapi BM25, `k1=1.5`, `b=0.75`
  (`/tmp/cognee-python/cognee/modules/retrieval/bm25_retriever.py:18-24`).
  IDF: `ln(1 + (N - df + 0.5) / (df + 0.5))` (`bm25_retriever.py:83-85`).
  Score: `Σ over unique query terms of idf(t) * (tf(t) * (k1+1)) / (tf(t) + k1*(1 - b + b*|chunk|/avg_len))`
  (`bm25_retriever.py:87-103`). Stop words: `DEFAULT_STOP_WORDS`
  (`/tmp/cognee-python/cognee/modules/retrieval/utils/stop_words.py:4`). This
  is **not** the existing Rust `LexicalRetriever`'s multiset-Jaccard scorer
  (`crates/search/src/retrievers/lexical_retriever.rs`) — a separate BM25
  lane, per-query corpus rebuild (no `OnceCell` reuse — contrast with
  `LexicalRetriever`'s `cached_chunks: OnceCell<Vec<CachedChunk>>` at
  `crates/search/src/retrievers/lexical_retriever.rs:29`, which is
  process-lifetime and intentionally *not* the pattern BM25 follows).
- **Batched neighborhood**: Python calls
  `graph_engine.get_neighborhood(entity_ids, depth=1)`
  (`/tmp/cognee-python/cognee/modules/retrieval/hybrid/entities.py:31`). The
  Rust `GraphDBTrait` addition must return true stored edge direction (not
  `get_connections`'s bidirectional-pattern treatment — see
  `crates/graph/src/ladybug.rs:2040-2070`'s "add both directions" test, which
  is exactly the flip this fixes).
- **`source_chunk_id` / uuid5 fallback**: node filter on summary hits reads
  `summary_payload.get("source_chunk_id")` and skips the summary-rank slot
  with a warning if absent
  (`/tmp/cognee-python/cognee/modules/retrieval/hybrid/pairs.py:44-51`); the
  id-derivation fallback that still recovers summary text is
  `uuid5(chunk_uuid, "TextSummary")`
  (`/tmp/cognee-python/cognee/modules/retrieval/hybrid/pairs.py:91-96`), used
  by `load_summary_text_for_ranked_pairs`
  (`/tmp/cognee-python/cognee/modules/retrieval/hybrid/chunks.py:212-230`).
- **`belongs_to_set` node filter**: a missing/non-list `belongs_to_set` on the
  payload makes `payload_matches_node_filter` return `False` whenever
  `node_name` is non-empty
  (`/tmp/cognee-python/cognee/modules/retrieval/hybrid/results.py:39-51`) —
  this is the exact mechanism behind the entity-search-returns-empty
  limitation, cited above.
- **`importance_weight` neutral default**: `DataPoint.feedback_weight`
  already defaults to `0.5` via `default_feedback_weight()`
  (`crates/models/src/data_point.rs:11-14`) — the new `importance_weight`
  field (landed by an earlier task in this phase, not P1-12) is expected to
  follow the identical `Option<f64>` + neutral-default pattern; confirm this
  at doc-edit time by reading `crates/models/src/data_point.rs` fresh (do not
  assume the field exists yet — as of this writing `importance_weight` lives
  only on `Data`/`DataPoint`'s ingestion-side sibling `crates/models/src/data.rs:58`,
  not yet on `DataPoint` itself; that migration is tracked by an earlier task
  in this phase, not this one).
- **Global context util absence**: Python's global-context lane depends on
  `get_memory_fragment` + a `GlobalContextSummary` node type
  (`/tmp/cognee-python/cognee/modules/retrieval/utils/global_context.py:1-53`);
  no Rust equivalent exists as of this writing (grep
  `crates/search/src/ crates/graph/src/` for `GlobalContextSummary` to
  reconfirm at execution time — it should still return zero hits until that
  util is ported).

## Testing

This is a documentation-only task — no Rust unit/integration tests are added
or modified. Verification is manual/grep-based:

1. `grep -rn "15 search modes\|15 retrieval strategies\|14 wire variants\|9 of the 14" docs/` after editing — must return **zero** stale hits (all four should now read 16/16/15/9-of-15 respectively).
2. Re-read each edited file end-to-end once after editing to confirm internal
   numbering (the renumbered `## 8`/`## 9` sections in `search.md`) is
   consistent and no section was duplicated or orphaned.
3. Confirm every new Python citation added (file:line) still points at real
   lines in `/tmp/cognee-python` by re-`sed -n`-ing them — Python's `main`
   branch can move between when this doc is written and when P1-12 executes.
4. Confirm the new `docs/http-server/routers/search.md` DTO field list
   textually matches P1-09's actual `SearchPayloadDTO` struct (`crates/http-server/src/dto/search.rs`)
   field-for-field — this is the one place a silent doc/code drift would be
   easy to miss since both are hand-written.

## Checkpoints / acceptance

- [x] All five files edited; each change matches an "Implementation steps" bullet above.
- [x] `docs/architecture.md`: crate bullet says 16 search modes; `get_neighborhood` documented on `GraphDBTrait`; rustdoc table row updated.
- [x] `docs/operations.md`: search subsection says 16 retrieval strategies and lists `HybridCompletion`.
- [x] `docs/configuration.md`: new "Search — hybrid retriever knobs" section present, with both the active-knob table and the Phase-2-reserved sub-list, cross-linked from `## HTTP server`.
- [x] `docs/tools/cli.md`: `HYBRID_COMPLETION` example added; pointer to the canonical enum list added.
- [x] `docs/http-server/routers/search.md`: new wire-table row, updated counts, `SearchOutput` comment, new DTO fields, and the full "Known limitations" section (all 3 sub-bullets) present; References section includes the new Python links.
- [x] Grep checks from Testing §1–4 all pass (no stale counts, no dangling citations).
- [x] `scripts/check_all.sh` still passes (sanity check per project convention — this task changes no Rust source, so it should be a no-op pass-through, but run it anyway to catch an accidental code paste or stray file).

## Risks & open items

- **Field-name drift vs. P1-09**: the wire knob names in this doc are
  predictions from the Python constructor signature, not read from landed
  Rust code (P1-09 doesn't exist yet at the time this plan was written).
  Highest-risk step in this task — do it last, after re-reading P1-09's diff.
- **`docs/http-server/routers/recall.md` not in scope**: if `HybridCompletion`
  becomes reachable from `recall`'s auto-routing (`query_router.rs`), that
  doc will also need a follow-up row/mention; explicitly out of scope here
  per the brief's file list — flag to the phase lead rather than silently
  expanding scope.
- **Section renumbering churn in `search.md`**: inserting "8. Known
  limitations" before "References" means every future edit to that file needs
  to remember the new numbering; consider (at execution time, not in this
  task) whether to switch that doc to unnumbered `##` headings to make this
  class of edit lower-friction going forward — noted here, not acted on.
- **No automated doc-consistency check exists** in this repo (`scripts/`
  has no markdown-link or doc-lint step) — the grep checks in Testing are the
  only guardrail; a stale count could still slip through if a reviewer skips
  them.
