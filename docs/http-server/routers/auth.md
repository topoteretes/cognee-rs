# Router: auth (login / logout / me)

> **Moved to closed.** The `auth` router (login/logout/me — fastapi-users-compatible
> JWT minting, cookie set, bearer return, `/me` lookup) was extracted to the
> closed companion crate `cognee-http-cloud`.
> See the [`cognee-cloud-rs`][cognee-cloud-rs] repo for the current
> documentation and source.
>
> [cognee-cloud-rs]: https://github.com/topoteretes/cognee-cloud-rs
