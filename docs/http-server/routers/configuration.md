# Router: configuration

> **Moved to closed.** The `configuration` router (per-user named JSON
> configuration blob store — KG schemas, ingestion templates, LLM system-prompt
> presets) was extracted to the closed companion crate `cognee-http-cloud`
>. See the [`cognee-cloud-rs`][cognee-cloud-rs] repo
> for the current documentation and source.
>
> [cognee-cloud-rs]: https://github.com/topoteretes/cognee-cloud-rs
