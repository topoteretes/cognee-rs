"use strict";
// Prebuilt native addon for linux-arm64-musl (aarch64-unknown-linux-musl).
// This file is the entry point for the @cognee-ts/neon-linux-arm64-musl optional dep.
// Consumers should not require this directly; import via the main 'cognee-ts' package.
module.exports = require("./cognee_ts_neon.node");
